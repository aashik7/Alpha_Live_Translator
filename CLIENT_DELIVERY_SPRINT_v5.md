# Alpha Live Translator — Client Delivery Sprint

**Version:** v5 (2026-08-10). **Supersedes `BUG_FIX_ROADMAP.md` v4 for
execution order and scope.** v4 remains the historical evidence record —
read it only when an item below points you there.

**Deadline: client delivery 2026-08-24.** This is a client project, not a
personal tool. It has to keep working when nobody is sitting next to it.

---

## 0. Agent: read this section first, every session

You are continuing work on a Windows desktop app that live-translates
meeting audio between Japanese and English. A long bug-fix campaign has
already run (Batches 1–3 complete, Batch 4 partially). **Do not restart
that campaign.** This file replaces its ordering and scope.

**Three rules that override everything else in this sprint:**

1. **No architecture changes.** No rewrite, no controller redesign, no
   three-store consolidation, no capture-layer change. Those are real and
   necessary work, and they are deferred to after delivery
   (§6). Attempting them inside 2 weeks is the single most
   likely way to miss the date.
2. **Never leave two authorities alive.** If a fix would mean the old
   path and the new path both running, stop and escalate to the human.
   Every "patched the dangerous symptoms instead" note in the existing
   reports is a place this rule would have fired.
3. **No drive-by fixes.** Notice something else broken → one line in §9
   and keep going. This project's ledger shows six separate cases where
   one fix unmasked the next; inside a 2-week window that cascade is the
   main risk to manage, not a curiosity.

---

## 1. Where the project is right now

**It largely works.** Recent live runs in both languages complete with
`final_status: completed`, `stop_finalize_failed: false`, translation
jobs 31/31 and 12/12, zero duplicate submissions, watchdog firing down
from 83% to 2–3%, Start-button freeze gone (8.8 s → 0.0 s), and the
revise path working for the first time (was 0 of 138 commits, now
observed). Full suite at that point: 354 tests, 5 failures + 2 errors +
2 skipped, same 7 names throughout — those 7 predate this engagement.
**Current suite is 777 tests** with the same 7 names; see §3 for the
authoritative baseline, which is the number to check against.

**What is still genuinely broken.** F was found 2026-08-11 via the first
real live-audio test this sprint has had (item 40) and is **more severe
than A for any English-source session** — flagged first, not relabeled
into the A-E order below because that order predates it and reordering
it silently would hide that this is new information, not always-known
priority. Human call on where it slots into §5's day plan; see §9.

| | Problem | Evidence |
|---|---|---|
| **F** | ~~pending~~ **FIXED and CONFIRMED on a second live run, 2026-08-11, item 51.** English commits self-concatenated on every reformatting variant Deepgram sends for the same growing utterance ("50 percent" then "50%", "mister" then "Mr."), and again on every slid window that repeated the previous chunk's tail — instead of replacing or joining. Corruption compounded every tick and landed in the canonical ledger, translation input, and final export alike | Reproduced against the real function from the **verified real input sequence** of run `...182940` utterance `U-1` (9 chunks, recovered and confirmed by re-merging them pre-fix to get the recorded corrupted line back byte-for-byte). Pre-fix that folded to **472 characters** with "olympia" ×4, "bodybuilder" ×6; post-fix **127 characters**, each phrase once, matching the reference transcript. Export-wide pre-fix: 5 of 54 lines carried **85.9%** of all characters, worst 5039 chars from ~112 glued fragments. Pre-existing, not new — same pattern on run `...133236` (2026-08-08). Japanese never reaches this function. Tests: `test_utterance_lifecycle_merge_lexical.py` (20). See §9 |
| **A** | ~~pending~~ **FIXED 2026-08-12, item 42.** Japanese sentences committed by the assembler were **overwritten in the canonical ledger by a later, textually unrelated commit that reused the same `canonical_utterance_id`** | Root cause proven in item 41 (`PROBLEM_A_ROOT_CAUSE.md`): the id-mint gate read a flag computed *before* the revision authority ran and never recomputed, so a candidate the authority had just judged a **new sentence** still reused the previous id, and the ledger revise replaced `final_text` in place. 10 real sentences lost across the recorded corpus. Fixed by requiring a revise to be non-destructive by content; a disjoint follow-up now gets its own record. Both halves are pinned: no loss **and** no duplication — see §9 |
| **B** | ~~pending~~ **FIXED 2026-08-12, item 43.** Real speech quarantined as `noise_fragment` and never recovered | Corpus re-measured 2026-08-12 and it corrects this row's old "2 of 2": **4 distinct fragments were dropped, 3 of them real Japanese speech** — `寝れた、幸せ、`, `。忘れちゃうし、`, `最近また` — with only a bare `、` being true noise. Quarantine no longer deletes: expired and stop-flushed fragments are committed flagged. Verified end-to-end on the realistic path (expiry mid-session, then more speech): the recovered fragment now reaches the ledger. See §9 |
| **C** | ~~pending~~ **FIXED 2026-08-12, items 22 + 23 + 33s.** Two speakers' turns could merge into one line | Reproduced directly before fixing: two *unknown* speakers produced `'the first speaker said this, a totally different remark'` on one line, and a speaker change on a held chunk produced `'speaker one is talking here, speaker two interrupts now'`. Both now start their own utterance. Guarded against over-tightening by `SameSpeakerStillMergesTest` — one speaker's continuous speech must still merge across 3 chunks. Replay decision counts on all 7 runs are **unchanged**, so nothing fragmented. See §9 |
| **D** | No behaviour defined for network drop, DeepL quota exhaustion, device change, or invalid credentials | Not covered by any of v4's 37 items |
| **E** | Verification depends on a human running live sessions | Why v4 items 10/11 sat unverified across five sessions |

F is the most severe single defect found this sprint for any English-source
client session — a wall of unreadable repeated text on nearly every
longer utterance, not a rare edge case. A and B are content loss on the
Japanese side. C is visible on stage. D is what breaks on a client's
machine when you are not there. E is why everything takes days instead
of minutes.

---

## 2. Orientation — do this at the start of every session

Shortened from v4 §1 deliberately. Do not run v4's full ritual; it costs
20–40 minutes per session and this sprint cannot afford it.

1. Derive the repo root from your working directory. **Do not hardcode
   it** — this repo has moved between Windows user accounts once already.
   Confirm this file and `Alpha_Live_Translator\` are both in it.
2. Read the repo root's `CLAUDE.md` for standing restrictions.
3. Read **this file only**. Do not read `BUG_FIX_ROADMAP.md` unless an
   item sends you there.
4. `git log --oneline -15`. Compare against §8's ledger. If they
   disagree, **fix this file first** and note it in §9.
5. Run the test baseline (§3). If it does not match §3's current numbers
   and names, stop and tell the human. (§3's numbers change as the sprint
   adds tests — read §3 itself, never a number cached here or in memory.)
6. Go to the current day in §5.

---

## 3. Project facts

**Test command**, from inside `Alpha_Live_Translator\`:

```bash
SKIP_TK_INTEGRATION_TESTS=1 "<repo_root>/.venv/Scripts/python.exe" -m unittest discover -s tests -p "test_*.py"
```

There is no pytest in this venv; `unittest discover` is the only runner.

**Baseline:** **777 tests**, **5 failures + 2 errors**, same 7 names. `test_item48_audio_manifest_bounded...test_interleaved_streams_each_keep_their_own_run` is an additional intermittent 8th name -- passes in isolation, unrelated to item 71.
Any change to that set means you broke something. (Was 354 at sprint
start; the growth is this sprint's own regression tests. Do not treat an
increase as suspicious — treat a change to the 7 *names* as suspicious.)

**Skips are 2 or 3, and that is expected.** Two are permanent — a Tk
display test and an obsolete pre-hotfix module. The third,
`test_english_line_grouping…test_words_survive_the_real_2342_character_line`,
`skipTest`s itself when run `...20260812-154956` is not on disk. Retention
has since deleted that run (43 recorded runs are down to 11), so it now
skips. **Verified 2026-08-15, not a regression** — but note the test's
guarantee disappears silently with the evidence, so do not read its pass
as ongoing coverage.

**One caveat, added 2026-08-11, resolved 2026-08-19.** The 7 names are the
*stable* set, but an 8th can appear — this has happened with at least two
different names now
(`test_item48_audio_manifest_bounded…test_interleaved_streams_each_keep_their_own_run`,
`test_stop_finalize_v3_2_3…test_phase_constants_match_spec`), so treat it
as a class of pre-existing suite flakiness, not one specific test.
`test_task9_report…test_inactivity_timeout_fallback_survives_immediate_real_stop_5x`
used to be the usual example (item 78) — **it is fixed as of `1bc1637`
and should no longer flake; if it fails now, that is a real regression,
not the old intermittent case.** For any *other* 8th name: if it is the
only new name and re-running clears it, treat as the known flakiness
class, not a regression. Any name that persists across re-runs, or any
change to the 7 stable names, is real. See §9.

**Venv:** `<repo_root>\.venv\Scripts\python.exe`. The bare system python
will fail with `ModuleNotFoundError`. If the venv reports
`did not find executable at ...`, repoint `home` / `executable` in
`pyvenv.cfg` to the local Python 3.14 — `.venv` is not relocatable and
this has already happened once.

**Never use `sed -i` on this repo's `.py` files.** It flips CRLF to LF and
produces full-file diffs. Use Edit.

**Locating code:** use grep anchors, never line numbers. v4's line
numbers went stale twice.

---

## 4. Method — every item, no exceptions

Carried from v4 §3, with two amendments marked **[v5]**.

1. **INVESTIGATE.** Grep the anchor, read the code, read the referenced
   run logs. If reality contradicts the item's description, **the
   description is wrong** — say so and log it in §9. Never force a fix to
   match a stale description.
2. **DESIGN.** Propose the exact change: what triggers the bug, why this
   addresses the root cause, what could go wrong, what stays unchanged.
3. **APPROVAL GATE. [v5]** Required for items marked `[gate]` in §8. For
   items following an established template, proceed without waiting.
4. **IMPLEMENT.** Only the approved change, only the named files.
5. **PROVE THE TEST CATCHES THE BUG.** Write the regression test, then
   temporarily disable your fix and confirm the test **fails** the way the
   original bug would. Restore, confirm it passes. **This step is not
   optional** — it caught a genuinely wrong test in item 14.
6. **VERIFY NO REGRESSION.** Full suite. Baseline must be exactly the
   same 7 names; skips 2 or 3 (see §3 — the third is data-dependent).
   **[v5]** Also run `tools/score_run.py` (item 39) against the
   reference runs.
7. **COMMIT.** One fix per commit, revertable on its own.
8. **UPDATE §8 AND §9 BEFORE ENDING YOUR TURN.** Commit hash, date, what
   the fix actually was, test file, model used. If you completed only
   some of steps 1–7, still record where you stopped and what you found.

**Rollback:** if step 5 or 6 fails and you cannot resolve it in the same
turn — `git checkout --` if uncommitted, `git revert <hash>` if
committed. Never amend or reset; history stays honest across agent
sessions. Record it in §9 and leave the item pending.

**Live-test protocol:** you cannot run the app. When a live session is
needed, tell the human exactly what to speak and for how long, ask for
the newest folder under `troubleshooting/runs/`, then analyse it
yourself. **Do not treat `coverage_ratio: 1.0` as proof nothing was
lost** — that gate only compares canonical → final, so a record lost
before reaching the ledger is invisible to it. That is exactly how
problem A hid. Compare assembler decision counts against ledger record
counts directly.

---

## 5. The plan — 14 days

### Days 1–2 (Aug 10–11) · Instrumentation — items 38, 39, 40

Nothing else starts until this is done. Right now every verification
costs a human live session plus manual log reading; that is why items
sat unverified for five sessions. Two days here buys back the other
twelve.

- **38** replay harness — **Opus 5** — DONE, `960f907` + `2d34a41`
- **39** run scorer — **Sonnet 5** — DONE, `410f6bf`
- **40** reference corpus + baseline numbers — **Sonnet 5** — **BLOCKED,
  needs one live session, see §8/§9.** Everything else in this phase
  does not wait on it.

**Gate:** ~~all 27 recorded runs replay~~ — **corrected 2026-08-10, see §9.**
Only **6** runs are replayable from `provider_events.jsonl` at all; the
other 21 carry no genuine provider ingress (10 have the file but only
post-lifecycle re-emissions, 11 lack the file). All 6 are Japanese —
English sessions record no raw ingress by design.

Gate is therefore a content requirement, not a count: **all 6 Japanese
runs with genuine provider ingress replay** — `...20260807-155922`,
`...20260807-160130`, `...20260807-160529`, `...20260808-134815`,
`...20260808-155334`, `...20260809-174516` — **and the recorded-side
loss measurement lands on all 6.** Measured 2026-08-11:

| Run | ingress fed | commits | dropped sentences |
|---|---|---|---|
| `...155922` | 3 | 3 | 1 |
| `...160130` | 11 | 4 | 2 |
| `...160529` | 25 | UI mints its own `canonical_utterance_id` | **Opus 5** | — | **CLOSED as WON'T FIX 2026-08-12, audited.** `jpm-utt-` is a separate namespace from the assembler's `jp-utt-`, Japanese **manual mode only**, and the current code is itself a deliberate earlier fix. Moving where identity is minted is §0 rule 2 territory: real risk, no observable defect |
| `...134815` | 50 | DeepL `context` parameter — previous 2–3 committed lines, unbilled | **Sonnet 5** | 12 | **DONE 2026-08-12.** Each line is translated stripped of everything said before it, so pronouns, honorifics and topic have nothing to resolve against. `context` gives DeepL that background; it neither translates nor bills it. Bounded by `TRANSLATION_CONTEXT_LINES=3` and `TRANSLATION_CONTEXT_MAX_CHARS=900`, same-language only, remembered only after a SUCCESSFUL translation. Support detected on both the SDK **and the client object** — see §9 |
| `...155334` | 71 | 32 | 6 |
| `...174516` | 82 | 36 | 0 |

~~"Loss pattern present: 160529 (−1), 134815 (−6), 155334 (−6). Zero loss:
155922, 174516."~~ — **superseded 2026-08-11 before it was written in.**
Those figures are the decisions-minus-ledger delta, which both
over-counts (a genuine `revise` is not a loss) and under-counts (an
overwritten id still has a ledger record). `...155922` was not zero-loss;
`...174516` is the only genuinely clean run. See §9.

**Replay does not reproduce any of the 14 losses** — that is a finding
under condition 4, not a gate failure. See §9 and item 38b.

### Days 3–6 (Aug 12–15) · Content integrity — items 41, 42, 43, 22, 23, 33s

The bugs that lose the client's words.

- **41** prove problem A's root cause against the fixture — **Fable 5,
  `/effort xhigh`**. Item 38 localised it: the assembler commits two
  textually disjoint sentences under one `canonical_utterance_id`, the
  ledger keys on that id, and the second commit overwrites the first.
  Item 38b then ruled out timing as the mechanism (real-timer replay
  fixes decision *counts* but still reproduces 0 of 14 losses) and left a
  specific, cross-validated lead in its place — **do not start 41 from
  scratch, start from this:**

  `japanese_sentence_assembler.py:3635` computes `update_previous_requested`
  from boundary-stabilizer signals *before* `decide_stable_revision_action`
  runs. That function's verdict (`final_revision_action`) can disagree —
  correctly recognise a candidate as a new, disjoint sentence
  (`"append"`) — and when it does, lines 3749–3756 reset
  `stable_layer_update_previous`/`post_update_previous` and even clear
  `metadata["boundary_should_revise"]`, but **`update_previous_requested`
  itself is never reassigned.** The id-mint gate at line ~3912
  (`proposed_action = "revise_previous" if update_previous_requested else
  "commit_new"`) reads that stale variable, not `final_revision_action`.
  Cross-checked against `logs/japanese_accuracy.log`'s
  `STABLE_REVISION_DECISION` events on all 6 runs: `update_previous_requested
  == True and final_action == "append"` matches 12 of 13 observed
  id-reuse events exactly (§9, 2026-08-11). **This is a lead, not a
  proof** — unverified: why the boundary stabilizer sets
  `update_previous_requested=True` on a candidate its own downstream
  check rejects, and whether fixing the read site (use
  `final_revision_action`) or the write site (boundary stabilizer) is
  correct. That determination is 41's actual job. Do not expect
  `replay_run.py`, real-timer or not, to reproduce this for you — drive
  `decide_stable_revision_action`/the boundary stabilizer directly, or
  work from `japanese_accuracy.log` on the recorded runs.
- **42** fix it: a commit that is not a revision of its target must
  never land on that target's id — no silent overwrite. An overwrite
  that is genuinely wanted must be a containment/extension of what it
  replaces, or it fails loudly with both texts in the log — **Fable 5**
- **43** make quarantine non-destructive — **Opus 5**
- **22, 23** speaker fail-open and Case B cross-speaker merge — **Opus 5**
- **33s** scoped: stop the relabeled speaker feeding the
  same-speaker-extension check — **Opus 5**

**Gate:** ~~run `...20260807-160529` replays with assembler decisions ==
ledger records == export lines~~ — **wrong equality, corrected
2026-08-11.** A genuine `revise` makes decisions exceed ledger records
legitimately (10 vs 9 on that run is one revision, not one loss), and an
overwritten id still produces a ledger record, so that equality is both
too strict and blind to the actual bug.

Replacement gate: **`tools/replay_run.py --all` reports 0 dropped
sentences on the recorded side of all 6 runs** (currently 14), each drop
independently confirmed against the export. Zero cross-speaker merges on
the reference corpus.

### Days 7–9 (Aug 16–18) · Resilience — items 44, 45, 46, 47, 48

This is the section that exists because it is a client project. On your
own machine you restart the app when something goes wrong. On theirs,
nobody will know what happened.

- **44** Deepgram reconnect mid-session — **Opus 5**
- **45** DeepL failure/quota degrade + circuit breaker — **Opus 5**
- **48** 60-minute stability: bounded queues, ledger memory, Tk line cap
  — **Opus 5**
- **46** credential failure surfaced at Start — **Sonnet 5**
- **47** connection status indicator — **Sonnet 5**

**Gate:** 60-minute session with a deliberate network drop ends
`completed` with zero content loss.

### Days 10–11 (Aug 19–20) · Validation and packaging — items 49, 11b-LT

- Full suite, replay all runs, score everything.
- **11b-LT**: the two Stop variants that have never executed live —
  (a) Stop immediately mid-sentence, (b) Stop after a ~10 s pause.
- **49** clean-machine install — **Sonnet 5**. The `.venv` has broken on
  a user-account move before; find that class of problem here.
- Three full sessions per direction **from the installed build**.

### Day 12 (Aug 21) · Code freeze

Tag `client-delivery-v1`. If buffer remains, item **50** (DeepL `context`
parameter) is the only optional addition — one API parameter,
unbilled characters, meaningful terminology consistency gain.

### Days 13–14 (Aug 22–23) · Rehearsal, zero code

Run the delivery scenario end to end on the actual machine. Two tagged
builds exist; if something breaks, demo the one that works.

### Before anything else, today

```bash
git tag -a known-good-v0 -m "Working build before sprint"
```

Package this commit and store the installer outside the working tree.
Fallback is not time — it is a tag. If at any point the tree is worse
than this and you cannot see why within 30 minutes, reset to it.

---

## 6. Explicitly out of scope until after 2026-08-24

Do not start these, do not partially start these, do not "just quickly"
do these:

Single canonical controller rewrite (v4 items 28–32) · three-store
consolidation (item 32) · dual-channel capture · dead-code removal
(item 36) · items 25, 27, 27b, 28a, 28b, 30, 35, 37 · Japanese
phrase-table cleanup · translation quality work beyond item 50 · the
summary feature.

**This list is the post-delivery backlog** — there is no separate
roadmap file, deliberately (see §9, 2026-08-11). Anything deferred gets
one line here, so there is exactly one place to look.

**Dual-channel capture is the highest-value item in it** — it makes
speaker identity a fact rather than a guess and deletes most of the
cross-speaker bug family. Cost impact is roughly $0.46 → $0.92 per audio
hour; the reason to defer is the 2-week window, not the money.

Also deferred here, added 2026-08-11:

- **English replay coverage.** `tools/replay_run.py` is Japanese-only by
  evidence, not choice: only `japanese_final_chunk_stabilizer.py` calls
  `record_raw_deepgram_final` on true ingress, so all 10 recorded English
  runs hold zero replayable rows. The `raw_deepgram_finals.jsonl`
  fallback adapter stays **rejected** — a second input adapter is two
  definitions of "replay input", which §0 rule 2 forbids. The real fix is
  upstream: record genuine ingress for English too, one format for both
  languages. Revisit only if items 44/48 turn out to need it.
- **WER gap vs. raw Deepgram.** The real accuracy measure. Record it at
  baseline and at delivery, but do not gate the client date on it —
  improving it is post-delivery work.

---

## 7. Definition of done for delivery

All of these, on the reference corpus and on three consecutive 30-minute
sessions per direction:

| # | Gate | Target |
|---|---|---|
| 1 | committed sentences dropped before the export (`replay_run.py`, recorded side) | 0, 3 runs |
| 2 | exact-duplicate lines in export | 0 |
| 3 | canonical records without a translation | 0 |
| 4 | lines containing two speakers' turns | 0 |
| 5 | quarantine events losing real speech | 0 |
| 6 | 60-min session with forced network drop | `completed`, zero loss |
| 7 | Start→Stop→Start, 5 cycles | no degradation |
| 8 | clean-machine install runs the full scenario | pass |

The WER gap vs. raw Deepgram is the real accuracy measure, and it is
deliberately **not** in the table above — record it at baseline and at
delivery, but do not gate the client date on it. See §6.

---

## 8. Item ledger — complete history and remaining work

Model column records what was actually used (completed) or is
recommended (pending). `[gate]` = human approval required before coding.

### Batch 1 — trivial, standalone · COMPLETE

| Item | What | Commit | Model | Status |
|---|---|---|---|---|
| 1 | Dead zero-arg `_flush_pending_translation_submit()` in `_begin_graceful_stop` | `6564b36` | Sonnet 5 | DONE |
| 2 | `INTERIM_GHOST_TTL_MS` 1500 → 6000, from measured 1924 ms en / 4063 ms ja | `9892cb1` | Sonnet 5 | DONE |
| 3 | `scan_interim_ghost_evidence.py` REVIEW verdict at ≥25% watchdog ratio | `38a92b5` | Sonnet 5 | DONE |

### Batch 2 — make silent failures loud · COMPLETE

| Item | What | Commit | Model | Status |
|---|---|---|---|---|
| 4 | `_remove_interim_line_from_display` mark guard (92→0 exceptions/run) | `ddeb67f` | Sonnet 5 | DONE |
| 5 | Log 3 silent excepts in `_on_store_segment_updated` | `5181b8c` | Sonnet 5 | DONE |
| 6 | Log `reset_for_session` + `_resolve_correction_target_locked` | `38c6096` | Sonnet 5 | DONE |
| 7 | Log remaining 6 silent excepts across 5 files | `07d5234` | Sonnet 5 | DONE |
| 7b | Japanese stabilizer failure no longer falls into English commit path | `6726f68` | Opus 5 | DONE |
| 8 | Log `_observe_identity` fail-open (behaviour unchanged) | `ec38779` | Sonnet 5 | DONE |
| 9 | Quarantine evidence scan (investigation only) | doc only | Sonnet 5 | DONE |
| 9b | Start-button freeze: audio cleanup made non-blocking (8.8 s → 0.0 s) | `b220c86` | Sonnet 5 | DONE |

### Batch 3 — identity and text-comparison hardening · COMPLETE

| Item | What | Commit | Model | Status |
|---|---|---|---|---|
| 9c | Translation-gap self-heal used the wrong id space | `13f20ca` | Opus 5 | DONE |
| 10 | `_check_stop_tail_duplicate` narrowed to equality-or-prefix | `b404c19` | Opus 5 | DONE |
| 11 | `_should_commit_interim_recovery` narrowed likewise | `b2b39de` | Opus 5 | DONE |
| 11b | Watchdog stashes orphaned interim before clearing | `69605cc` | Opus 5 | DONE |
| 11c | Stop-tail min length split: CJK 4 / Latin 20 (from 2210 interims) | `c43f57b` | Opus 5 | DONE |
| 12 | `_should_repair_previous_segment` containment narrowed | `af6781e` | Sonnet 5 | DONE |
| 13 | Substitution corrections update instead of duplicating, id-only | `8f19afe` | Opus 5 | DONE |
| 14 | `merge_japanese_fragments` silent whole-fragment discard removed | `5ffb18d` | Sonnet 5 | DONE |
| 15 | 5 ordinary connectives removed from speaker-lock prefixes | `5ffb18d` | Sonnet 5 | DONE |
| 16 | `teams_commit_decision_from_dup_action` confirmed diagnostic-only | `5ffb18d` | Sonnet 5 | DONE |
| 17 | Unsafe store read/write variants retired, 4 call sites | `43374ad` `5d4578a` | Opus 5 | DONE |
| 18 | `add_translation` matches on `canonical_utterance_id` | `0aa6a8f` | Sonnet 5 | DONE |
| 19 | `duplicate_continuation_ratio` narrowed to prefix-or-suffix | `0aa6a8f` | Sonnet 5 | DONE |
| 19b | `merge_pending_and_current` appends instead of revising | `ac8feb5` | Opus 5 | DONE |
| 20 | Per-utterance `event_id` (was the connection-level constant) | `3a59f6d` | Sonnet 5 | DONE |
| — | Stop-tail end-to-end harness, 17 tests | `9dae426` | Opus 5 | DONE |

### Batch 4 — concurrency and state-machine safety · PARTIAL

| Item | What | Commit | Model | Status |
|---|---|---|---|---|
| 20b | Japanese appends no longer set a self-referential `revision_target_id` | `2367285` | Opus 5 | DONE |
| 20c | 5th containment site in `decide_transcript_action` narrowed | `012695d` | Opus 5 | DONE |
| 21 | `_confirm_transcript_commits` verdict actually gates run status | `0ed9991` | Opus 5 | DONE |
| 22 | Unknown speakers both default to `1` (fail-open); use `speakers_confirmed_same()` | **Opus 5** | 5 | **DONE 2026-08-12.** `_compatible_with_active_locked` now normalises unknown speakers (`None`/`0`/`""`) to `None` via `_known_speaker` and compares with the fail-closed `speakers_confirmed_same`. Swapping the call in alone would **not** have been enough — it reads `0 == 0` as a confirmed match, so the normalisation is load-bearing |
| 23 | Case B merges across channel/speaker; align to Case C's gating | **Opus 5** | 5 | **DONE 2026-08-12.** `force_new` was `not same_active and active is not None and active.committed` — so an incompatible candidate merged whenever the active utterance was *uncommitted*, which is the normal state for a held final chunk. Now `active is not None and (active.committed or not same_active)`, matching Case C. **22 and 23 interlock** — see §9 |
| 24 | `revise_last_transcript_snapshot` has zero speaker check | — | **Opus 5** | **DONE 2026-08-12.** Third sibling of items 22/23. It accepted `speaker` but never compared it — the argument was only a fallback value — so it revised whatever the literal last row was. Now fail-closed via the same primitive `transcript_store` has used since TASK_2E_FINDINGS.md item 3; a speaker change appends instead of overwriting. **Was orphaned in this file** — marked SPRINT day 6 in §8 but absent from §5's day plan, so it would have been skipped. Tests `tests/test_v4_items_24_and_26.py` |
| 11b-LT | Verification only: Stop after ~10 s pause, never executed live | **Opus 5** | 10 | **PASSED 2026-08-14.** Run `...20260814-165109` (33 s): user spoke, paused ~10 s, pressed Stop. Last commit `speech_final` at 16:51:27, Stop ~15 s later, and the final sentence — "Test if the last sentence is able to capture." — is present in the export. `incomplete_stop_tail.txt` **empty**, no suppression, `stop_finalize_failed: False`, 2 pub → 2 canonical → 2 exported |
| 25 | UI mints its own `canonical_utterance_id` | — | Opus 5 | **INVESTIGATED 2026-08-12, stays DEFERRED — with reasons.** Real: `main_window.py` mints `jpm-utt-{uuid4}` in three places. But it is a **separate namespace** (`jpm-utt-` vs the assembler's `jp-utt-`), so it cannot collide with the ids item 42's gate manages; it is **Japanese manual mode only**, not the live-session path; and the current code carries a comment showing it was a deliberate fix (carry real identity rather than none). Moving where identity is minted is exactly what §0 rule 2 wants done carefully. Not a delivery blocker |
| 26 | Ledger has no internal version guard | — | Opus 5 | **DONE 2026-08-12.** `_revise_record_unlocked` overwrote `final_text` in place with no `source_version` comparison; ordering was enforced only by callers. Item 42 fixed that class of loss at the caller — this is the authority defending itself, so a *different* caller cannot repeat problem A. Deliberately an **ordering** guard ("is this write older than what the record holds?"), not a content guard — item 42 owns the non-destructive question, and duplicating it here would be two authorities (§0 rule 2). Absent versions pass through unchanged |
| 27 | `_observe_identity` fail-open: decide from Batch 2 evidence | **Opus 5** | — | **CLOSED 2026-08-12 — evidence says leave it.** The item asked for a decision from evidence. Measured across **all 43 recorded runs: `OBSERVE_IDENTITY_FAILED_OPEN` fired 0 times.** Fail-closed here means REJECTING a commit, i.e. content loss, so switching it would trade a never-observed risk for a real one. No code change |
| 27b | `_textually_related_revision` threshold tuned while lineage was constant-true | **Opus 5** | — | **STAYS DEFERRED 2026-08-12, audited.** The premise holds — 0.72 / 0.55–1.85 were set under conditions that no longer apply — but re-deriving them needs a labelled corpus that does not exist. Tuning blind is exactly what item 34 was superseded for ("n=2 is too small to tune a threshold") |

### Batch 5 — single canonical controller · DEFERRED, except the scoped items below

| Item | What | Model | Status |
|---|---|---|---|
| 33s | **Scoped**: stop relabeled speaker feeding same-speaker extension | **Opus 5** | 6 | **DONE 2026-08-12.** `_publish_sentence` captures `boundary_speaker` (the raw provider label) *before* `_resolve_output_speaker` relabels for display, and passes that to `decide_stable_revision_action`. Closes a feedback loop: one of the stabiliser's own lock reasons is "text reads like a continuation", so its output was manufacturing the same-speaker agreement the guard exists to test. **Not observable today** — the guard is already fail-closed (item 41) — this removes the trap |
| 34 | `noise_fragment` threshold from evidence | — | SUPERSEDED by item 43 |
| 28, 29 | Fixture capture + root-cause proof for problem A | — | ABSORBED into items 41, 42 |
| 28a, 28b, 30, 31, 32, 33 (full), 35, 36, 37 | Controller rewrite, store consolidation, key redesign, dead code | Fable 5 / Opus 5 | DEFERRED — see §6 |

### New items — this sprint

| Item | What | Model | Day | Status |
|---|---|---|---|---|
| 38 | `tools/replay_run.py` — replay a recorded run's `provider_events.jsonl` headlessly, diff against its FINAL export | **Opus 5** | 1–2 | **DONE** — gate resolved 2026-08-11, all 5 conditions met. Tests `tests/test_replay_run_verdict.py` (10). Recorded-side measurement is the deliverable and it proved problem A's mechanism (id collision, 14 sentences, 505 chars, 14/14 confirmed absent from export). Replay side does **not** reproduce it — finding under condition 4, carried to item 38b |
| 38b | Real-timer replay: drive the assembler through `LanguagePipelineWorker` scheduling instead of fast-feed | **Opus 5** | — | **DONE 2026-08-11.** `tools/replay_run.py --real-timer`, tests in `test_replay_run_verdict.py::RecordedGapsTest` (pure-function; the real-timer replay itself is a slow opt-in tool, not suite-run). Result: fixes decision-count divergence (was 13 vs 29 etc., now matches within 1 on every run) but **still reproduces 0 of 14 content losses.** Timing was not problem A's mechanism. Left a cross-validated lead for item 41, see §9 and item 41's rewritten description |
| 39 | `tools/score_run.py` — pass/fail on the §7 gates plus latency percentiles | **Sonnet 5** | 2 | **DONE 2026-08-11.** Tests in `test_score_run.py` (17). Gates 1–3 are real verdicts; gates 4–5 need `--reference` (item 40) or report `NOT_MEASURABLE`/`HEURISTIC`, never a fabricated pass; gates 6–8 always `NOT_MEASURABLE` (live-session/installer properties, not a run folder's). See §9 for what it found on the existing 27 runs |
| 40 | Reference corpus: 10 min ja + 10 min en, hand-written expected transcript, baseline recorded | **Sonnet 5** | 2 | **DONE 2026-08-11.** Human ran a live ja session (`...20260811-175628`) and a live en session (`...20260811-182940`), each played back a public YouTube video with its own accurate captions/transcript as the reference — a clean way to get verbatim ground truth without needing the reference hand-typed live. References installed at `Alpha_Benchmark_References/current_{japanese,english}_actual.txt` (stale 2026-07-24 ones archived to `archive_20260724/`, not deleted). Scored with the existing `analyze_alpha_vs_reference.py`. Japanese baseline: `unaligned_alpha_line_ratio` 0.31, `cumulative_duplicate_count` 0 — usable. **English scoring surfaced problem F** (new row in §1) before the reference-comparison numbers could even be trusted — `analyze_alpha_vs_reference.py`'s own duplicate detector reads 0/false on this run despite the corruption, so its English CER numbers are not reliable either; direct counts in §9 instead. See §9 for the full trail |
| 41 | Prove problem A's root cause against run `...20260807-160529` — prove, do not assume | **Opus 5** (actually used) | 3–4 | **DONE 2026-08-12 — ROOT CAUSE PROVEN.** Supersedes the 2026-08-11 "strongly indicated, not proven" verdict, which was correct when written. Proof: `PROBLEM_A_ROOT_CAUSE.md` (repo root; `troubleshooting/` is gitignored). Phase 5 fixture now exists: `Alpha_Live_Translator/tools/reproduce_problem_a.py` — deterministic, headless, single-variable, **exits 1 today**. Two things closed the gap: driving `_publish_sentence` instead of `_route_stable_publish` (the layer above merges the texts first, making the revise harmless), and finding that `previous_record` is built with **no `speaker` key**, which makes the revision-decision engine fail-closed to `append` on every non-first commit (105/111 measured) — so Rules A–F have never executed. **Item 42 brief is §8 of the proof doc.** No `.py` modified by item 41 |
| 42 | Fix problem A: a commit that is not a revision of its target must never land on that target's id | **Opus 5** (actually used) | 4–5 | **DONE 2026-08-12.** Gate approved before implementation. The id-mint gate no longer reads the stale `update_previous_requested` alone — a revise now also has to be **non-destructive by content** (`_revision_is_non_destructive`), which is the discriminator item 41 measured as exceptionless. Blocked revises log `DESTRUCTIVE_REVISE_BLOCKED_NEW_UTTERANCE_ID` and mint a fresh id instead of silently overwriting. Tests `tests/test_problem_a_destructive_revise.py` (11), proven to fail on reverted code. `tools/reproduce_problem_a.py` now exits **0** (was 1). Suite 421, same 7 names |
| 43 | Quarantine becomes non-destructive: flag and commit, never drop. Supersedes item 34 (n=2 is too small to tune a threshold) | **Opus 5** (actually used) | 6 | **DONE 2026-08-12.** Both silent drop sites now recover instead of discarding: expiry queues to `_quarantine_recovery_pending` and commits **outside the assembler lock** (the worker holds it, so an inline commit would deadlock); stop-flush commits every real-content fragment rather than only "valid short list terms". Flagged `quarantine_recovered`. **One class deliberately still not committed** — text with no word characters, because that trips `accept_boundary_proposal`'s `empty_text` failure which sets the session-killing commit gate; it is logged `NOISE_FRAGMENT_NOT_COMMITTABLE`, never silently dropped. Tests `tests/test_quarantine_non_destructive.py` (8), both sites proven independently on reverted code. Suite 429, same 7 names |
| 44 | Deepgram websocket reconnect mid-session: backoff, buffer, commit in-flight, mark the gap visibly | **Opus 5** | 7 | **CLOSED 2026-08-12, all four requirements.** **backoff** — retry loop 1s→30s, per-attempt except, 1s floor. **buffer** — `_dg_replay_buffer` drained on the dead socket, replayed first on the new one. **commit in-flight** — was NEVER implemented; an utterance open when the socket died was neither merged (timing correctly rejects the restarted clock) nor committed (`force_new` replaced it outright), so every reconnect silently dropped it. New `commit_in_flight()`, called on the first unexpected close, before the marker. **mark the gap** — emitted from `_deepgram_on_open`, live-verified at 30.7s against a ~32s outage. Plus the keepalive's own `_send_ping` crash, guarded. Live-verified on runs `...154956` and `...161651`: both dropped, both recovered, 0 crashes, no record loss |
| 45 | DeepL failure/quota: degrade visibly, retry with backoff, circuit-break after N, never block the transcript | **Opus 5** | 8 | **REOPENED and FIXED 2026-08-12.** The retry machinery was real but unreachable for the case that matters: `deepl.ConnectionException` subclasses `DeepLException`, so it fell through to the catch-all `retryable=False` branch. Run `...142447`: the session's single translation request went out at 14:30:01 — during the WiFi drop — and died `permanently_failed` with **`retry_count: 0`**; `successful_translations: 0`. Connection failures are now classified retryable, checked on the exception TYPE **before** the message-substring rules (`"auth" in low` is broad enough to swallow a connection error). Quota and auth stay permanent |
| 46 | Invalid/expired credentials produce a clear message at Start, not a stack trace | **Opus 5** (actually used) | 9 | **DONE 2026-08-12.** `alpha/utils/service_status.py::preflight_credentials` returns structured, actionable problems. Deepgram **blocks Start** (no key = no transcript); DeepL does **not** (transcript-only is degraded, not broken — refusing to start would be worse). Missing vs placeholder are distinguished because they need different advice. The runtime half — a key valid at Start and later revoked — surfaces via `deepgram_auth_failed` as a `failed` state with advice, not an endless reconnect loop. Tests `tests/test_service_status.py` |
| 47 | Status indicator: connected / reconnecting / degraded / failed | **Opus 5** (actually used) | 9 | **DONE 2026-08-12.** `service_status.describe_connection()` collapses the live signals into one state. **Severity-ordered, not first-match** — several signals are routinely true at once. Note the ordering differs from this row's wording: `reconnecting` outranks `degraded`, because degraded means only translation is failing while the transcript still flows, whereas reconnecting means the transcript itself has stopped. Consumes item 45's `degraded` and item 44's gap seconds. Pure function, so the rules are testable without a Tk display |
| 46 + 47 | **REOPENED 2026-08-16 — both shipped as library code with NO CALLER** | **Opus 5** | — | **OPEN.** Found while scoping item 73. `alpha/utils/service_status.py` is imported by **nothing in the application**: grepping all six exported names (`preflight_credentials`, `blocking_problems`, `preflight_summary`, `describe_connection`, `ConnectionStatus`, `CredentialProblem`) across `alpha/` and `main.py` returns **0 non-defining references**. It is a module plus `tests/test_service_status.py`, and no more. So no credential check runs at Start and no status indicator exists in the UI — the operator sees neither. The functions themselves are correct and tested; what is missing is the wiring: a call at Start that can block it, and a widget that reflects `describe_connection`'s state. **This is the third instance of the same shape in this project** — item 44's `commit_in_flight` (never implemented), item 65's log (behind a gate that returned first), and now these two. **Before closing any remaining item, grep for a production caller, not just for the code.** |
| 67 | Gap marker is not in the exported transcript | **Opus 5** | — | **DONE 2026-08-14.** The marker cannot become a canonical record — synthetic, no `source_raw_event_ids`, and `RAW_EVENT_LINEAGE_REQUIRED` is worth keeping — so gaps are recorded BESIDE the ledger (`record_connection_gap`, run-scoped, cleared by `reset_for_run`) and rendered at export time. Prepended into the entry of the record that FOLLOWS the outage, which keeps `lines` 1:1 with `record_ids`; a standalone line would shift every coverage gate that pairs those lists by index. A gap after the last commit is appended rather than dropped. No gap → export byte-identical |
| 68 | Translation UI shows `Speaker: … ⏳` placeholder rows to the user | **Opus 5** | — | **DONE 2026-08-14.** Verified first that the row is NOT an ordering slot: `_clear_translation_loading_item` deletes it and appends the finished text at `tk.END` regardless, and ordering comes from the worker's `translation_sequence` buffer. Gated behind `TRANSLATION_PENDING_PLACEHOLDER_VISIBLE = False`. **The Tk mark is gated with the text**, not left behind — see §9. Registry entries unchanged, so counts and revision tracking are untouched |
| 69 | Live interim preview line is unbounded | **Opus 5** | — | **DONE 2026-08-14.** `_interim_preview_lines` reuses `english_line_grouping`, yielding `(text, is_last)` so only the final line carries the ⏳. English only; falls back to a single line on ANY failure, because this runs on the UI thread. Inherently safe: the preview is deleted `interim_anchor` → `end` and rewritten every tick |
| 70 | English text settles too rarely for a live meeting | **Opus 5** | — | **DONE 2026-08-19 (`61f3298`).** The flush now splits at an INTERIOR terminator, not only at the prev/curr seam. Reproduced first, against the real lifecycle: re-grown as cumulative windows the 25 longest real English records went in as 25 and came out as **25, longest 1982 chars, zero splits**. After the fix, same replay: **443 records, longest 476, 2 still over 400, 0 words lost, 0 duplicated.** The split takes the FIRST position where four conditions hold — terminator with text both sides; `head + " " + tail == merged` byte-exact; **`curr.startswith(head)`**, i.e. the provider's own latest text still carries the sentence; and `prev.startswith(head)`, i.e. it survived a merge verbatim. The third is what the withdrawn attempt lacked and is the only one that refuses a terminator the provider already withdrew (`_merge_lexical` leaves a stale period in `"I am very happy. today."` — the other three all pass on it). Both withdrawn-attempt traps are FIXED, not avoided: duplication now uses an exact committed-prefix strip instead of item 66's fuzzy `min_run=3` (which let `"Yes."`/`"Okay."` through), and `sentence_boundary_flush` joins `provider_disconnected` in `_HARD_BOUNDARY_COMMIT_REASONS` so the re-send trim cannot run on a split tail. **Two defects found by driving the real commit, neither visible from reading:** shortening `active.text` without bumping `active.version` made `_observe_identity` refuse EVERY interior split as `conflicting_same_version_text`, silently; and once splits began, item 66's fuzzy trim deleted genuine speech — a real record with `"So everybody agrees. So everybody agrees."` lost one. Each part verified by neutralising it alone (see §9). Tests `tests/test_item70_interior_sentence_split.py` (18); the 11 guards in `test_item70_cumulative_sentence_flush.py` all still pass. Suite 777 → 795, same 7 names. **Not yet seen in a live run — needs one English session to confirm on real provider timing.** History below kept per §4 step 1: **detection half shipped `22aafad`; the cumulative widening was implemented, adversarially reviewed, and WITHDRAWN.** The original description below is wrong on two counts and must not be worked from. **(a)** The flush is not rarely triggered — it is the *dominant* commit reason, 115 of 160 on run `...20260814-114309` and 160 of 221 across six English runs. It fires on most LINES while missing most BOUNDARIES: 921 sentences, 115 flushes (~1 in 8), mean `source_version` 29.8. Rate is 1.6–4.2 commits/min, not ~1/min. **(b)** Widening to the cumulative shape does not fix the symptom: on replay, longest record unchanged at 2084 chars, records >400 went 59→60. **The real obstacle, and where the remaining value is: 68% of Deepgram's finals do not end on a terminator**, so the boundary sits in the MIDDLE of the accumulated text while the gate tests `prev[-1]`. A fix must split at an INTERIOR terminator. Two reproduced traps are pinned in `tests/test_item70_cumulative_sentence_flush.py` and must keep passing: a ≤2-token sentence re-enters the next cumulative window and commits twice (item 66's `min_run=3` fails closed), and the flush tail handed to `_trim_resent_tail_locked` deletes an anadiplosis head outright |
| 70 | *(original description, superseded — kept per §4 step 1)* | — | — | ~~Root cause: Deepgram sends CUMULATIVE growing text, so `_flush_sentence_boundary_locked` (which only fires on a pure append) rarely triggers; growth arrives through the REPLACE path instead~~ |
| 71 | UI layout: translation primary at 70%, original transcript a 30% reference pane, meeting summary behind its button | **Opus 5** | — | **PHASES 1-3e DONE (`c108136`, `254a4fc`, `17dc560`, `aa7c20e`, `400e78a`, `870f7f7`, `65501ee`). Phases 4-6 OPEN and filed as items 74-77 for approval.** Phase 3b was a REWORK after the user tested the build and reported the footer misaligned, the buttons clipping into each other when shrunk, the start button missing in the compact layout, the pane ratio wrong and the title carrying a version number — see §9 for the four measured causes. Delivered: (1) `FONT_FAMILY` was `"Segoe UI Variable"`, which Tk resolves to **Arial** silently — `tkfont.Font(family="Segoe UI Variable").actual("family")` returns `Arial`, so no fallback ever fired and the whole app rendered in Arial; fixed to `"Segoe UI"` plus 16 additive design tokens. (2) Three-column reading grid, weights **derived** from which panes are visible rather than hardcoded per branch; both reference panes start hidden, created eagerly and `grid_remove()`d (C2). Below 700 px the columns become rows, matching the design's own media query. `_apply_left_column_panel_weights` lost its body — it wrote row weights on a container whose children are now in columns, which raises nothing and does nothing. (3) The design's type scale, with three measured facts pinned in tests. **`attr_name` deliberately unchanged on both panes** — `_transcript_box()` returns `initial_verse_box` and is the target of every transcript insert, so swapping the names would route transcript text into the translation widget and the widget-read export fallback would write it into the delivered file as translation output. **Not verifiable here:** hide/show *transitions* and stacked reflow. This environment reflows on first paint but not after a later `grid_remove()` (a sibling's width does not recompute), so every geometry test measures a fresh paint. Per `UI_REDESIGN_PROMPT.md`, a green suite is not evidence for this file. **3d/3e were verified live regardless** -- driven against the real running app at multiple widths after a second and third round of user reports found the header not responsive, the footer's Start/Stop taller than the other three buttons, and (traced deeper) the header's whole mode-switching chain comparing a design-px threshold against device px — see §9 |
| 71 | *(original filing, kept — the spec arrived later and moved the TRANSCRIPT to the right at 30%, not the translation)* | — | — | **OPEN — awaiting the user's detailed spec.** `show_meeting_summary` is already a button command (`main_window.py:8819`), so most of the wiring exists. **Trap:** do NOT lazily create the summary widget — background threads and stop/finalize reference widgets by attribute, and a missing one raises on a non-UI thread (the shape that escalated the `_send_ping` crash). Create eagerly, hide with `grid_remove()`. Only 2 test files touch Tk and both are skipped, so this is live-test-only |
| 74 | UI Phase 3: render-cap arithmetic, single-line deletes, batch-flush time budget | **Opus 5** | — | **DONE 2026-08-19 (`2813e22`), and the filing's severity was WRONG.** All three parts were measured against the real code and all three are **LATENT, not existing defects**: (a) probed at 620 segments against a cap of 500, the widget held exactly 500 lines with **zero drift** -- the trim removes one logical line per segment and a segment writes exactly one; (b) a completed translation likewise occupies exactly one logical line; (c) a real segment insert costs **0.009 ms**, so 8 items is 0.07 ms against a 10 ms budget -- about **2700x of headroom**. Each becomes live only when item 75 adds a second logical line or a per-entry widget (its own measurement was 35.6 ms per card, vs the ~25 ms per item where the flush would outlast its 200 ms interval). **(c) is FIXED** -- the flush now checks elapsed time inside the loop, re-buffers everything unrendered in order, always renders at least one item, and logs `TRANSCRIPT_UI_FLUSH_TIME_BUDGET_EXCEEDED` rather than trimming silently. **(a) and (b) are deliberately NOT rewritten** -- speculatively re-deriving arithmetic for a shape that does not exist would be the drive-by §0 rule 3 forbids. Instead the one-logical-line invariant both depend on is now pinned by tests, so item 75 fails loudly instead of silently. Tests: `test_item74_flush_time_budget.py` (7, four fail pre-fix). **The real live bugs in this area were three Tk anchor marks item 74 never named** -- see the separate rows and §9. Original filing kept below per §4 step 1: **filed 2026-08-18 as the risky remainder of item 71. Low risk and net-negative — these are existing defects, and item 75 cannot be done safely before them.** (a) `box.delete("1.0", "2.0")` runs `excess` times while `_displayed_segment_count` increments by 1 per segment; it only works because an entry is exactly one logical line. (b) The two translation revision deletes remove `mark -> mark lineend + 1 chars`, again one logical line, then pop the registry so nothing can reclaim an orphan. (c) `_flush_transcript_ui_batch` iterates its batch with no elapsed check; the drain loop's budget test is evaluated *before* `transcript_queue.get()`, so it bounds how many items start, not how long the tick runs — and the backpressure branch *raises* `max_per_poll` to 24 when the queue is already deep |
| 75 | UI Phase 4: per-entry model — speaker/time meta row and current-entry highlight | **Opus 5** | — | **OPEN, filed 2026-08-18. Medium risk. Blocked on item 74.** This is the first change that adds a **logical line**, which is the single thing that breaks the arithmetic above — font, wrap and spacing do not. Render as `tk.Text` tag ranges, **not** per-entry widgets: measured, 430 CTk cards cost 15.33 s and +49.3 MB against 38.46 ms for a full text rewrite, and one card is 3.6x the entire 10 ms per-tick budget. Two traps: `_update_interim_line_only` sets `interim_anchor` at `"end"` -- which was itself BROKEN and fixed 2026-08-19 (`f59b99c`): the default right gravity put appended text OUTSIDE `delete("interim_anchor", "end")`, not inside, so the preview was never removed and simply stacked. Use the corrected recipe (`"end-1c"` + LEFT gravity + re-established empty last line) for any new anchor; and `_get_translated_transcript_for_copy_export` falls back to `box.get("1.0", "end")`, so any decorative row rendered in the translation pane lands verbatim in the client's delivered file |
| 76 | UI Phase 5b: meeting summary as a modal overlay | **Opus 5** | — | **OPEN, filed 2026-08-18. Medium risk. Not needed for the client's request — item 71 already made the summary button-gated; this is only the overlay presentation.** Would be the file's first use of `.place()` (currently zero occurrences). **Do not add `grab_set()`:** a widget grab plus the 18 remaining `tkinter.messagebox` call sites can wedge the Tk thread, stopping `_process_ui_queue_once` from draining `transcript_queue`; if a Stop lands in that window `flush_pending_translation_submissions` blocks on its 2 s wait and segments committed just before Stop are never enqueued for translation |
| 77 | UI Phase 6: draggable divider, toast, interface EN/日本語 toggle | **Opus 5** | — | **OPEN, filed 2026-08-18. HIGH risk, net-new code with no existing pattern. Recommend NOT before 2026-08-24.** Divider: the obstacle is not `PanedWindow` (which measurably rejects a `CTkScrollableFrame`) but that `_apply_content_layout` re-derives the column weights on every resize and on every pane toggle, so any drag position stored as a weight is silently reset — it would need a persisted ratio first. The design's own sizing also conflicts below ~528 px, where Tk honours `minsize` 400 over weight. Toast: none exists; needs `_toast_job` added to `_stop_ui_loops` or it fires across `destroy()`, where `configure()` raises `TclError` and the excepthook records `CRASH_HOOK_TRIGGERED` (baseline 0). Interface toggle is genuinely new — `_build_language_profile` is translation routing, not UI labelling |
| 79 | English records commit mid-phrase, so most lines are not sentences | **Opus 5** | — | **OPEN, filed 2026-08-19 from the live run `...20260819-173950`.** Only **57 of 153** exported records end on a sentence terminator. **48 of the run's 84 short (<=5 word) lines carry no terminator at all** -- `'learn about'`, `'and it, and to uncover'`, `'fired by'` -- so they are NOT item 70's split, which requires one. They are mid-phrase commits from the `utterance_end` / `speech_final` / inactivity paths, which mean *speech paused*, not *a sentence finished*. Pre-existing and independent of item 70; it became visible because item 70 made every other line readable. The readable layer cannot repair it: `group_sentences_into_lines` works WITHIN a record, and a record that is half a phrase has nothing to group. A fix has to either hold a mid-phrase commit open until a terminator arrives (risking the loss item 44 exists to prevent) or group ACROSS records at render/export time. **Do not attempt before 2026-08-24.** |
| 78 | `test_task9_report` real-thread stop test is flaky | **Sonnet 5** | — | **DONE 2026-08-19 (see session log).** Filed 2026-08-18 while verifying item 71 Phase 2. Known since 2026-08-11 (§9) but never given an item — that entry left it as "a human call", so here it is as something to approve. `Issue3RealThreadIntegrationTest.test_inactivity_timeout_fallback_survives_immediate_real_stop_5x` failed non-deterministically on a varying iteration index, reporting `utterance_reconstruction` and `final_export` as failed required steps. **Corrected the 2026-08-11 entry on one point:** that entry concluded it is "flaky under full-suite load" because the module passed 9/9 in 3 runs out of 3 alone. Re-measured with more runs, it failed **3 of 8 alone**, so load was not the trigger and 3 runs was simply too few to see it. Attribution ruled out for item 71 by measuring both trees: 3 failures in 8 runs before the change, 2 in 8 after. **Root cause, found by driving the real host, not by reading:** `RealIntegrationHost` never wired `_flush_pending_transcript_queue` / `drain_transcript_queue_for_stop` onto itself, so `drain_stop_queues_on_main_thread`'s two `hasattr`-gated drain calls were both silently `False` and the Stop UI barrier degraded to a bare `transcript_queue.qsize()` read that flushed nothing — the only thing that ever emptied the queue on this host was `process_ui_queue()`'s own independent 100ms poll, racing the ~90-100ms stop sequence, a near coin flip matching the measured 3-of-8 rate. Fixed by borrowing the same real `AlphaApp` methods the class already borrows for everything else, plus the batch-buffer state they need. Verified with the real test across 10 separate fresh-process invocations (50 subtest-iterations), 0 failures, and the full suite unchanged on both trees (777, 6F+2E+2S, same names, confirmed by diffing a stash of the fix against HEAD) — see session log |
| 72 | Connection-gap markers are dropped when a session commits no record | **Sonnet 5** | — | **OPEN, filed 2026-08-15 by review of item 67.** `canonical_transcript_ledger.serialize_export_payload`, `grep: if pending_gaps and lines:`. The trailing-gap branch requires a non-empty `lines`, so a session whose network died before anything committed exports an empty file rather than the marker. Proven against the real function (see §9). Small and self-contained; the fix is to emit the merged marker as its own entry when there is no record to attach it to — but `lines` must stay 1:1 with `record_ids` (every downstream coverage gate pairs them by index), so a standalone marker line needs a matching placeholder id or a separate field, not a bare append **DONE `df1ecc1`.** Separate field `unattached_gap_line`; the caller prepends it. A second constraint was found while implementing and decided the shape: `text` must stay **empty**, because `run_artifacts.py`'s `if not text.strip(): text, source_name = _committed_final_source_text(host)` is real content recovery (snapshot → host → transcript store). Returning the marker as `text` would satisfy that check and silently switch recovery OFF, exporting a one-line marker in place of speech the store still held — a reporting fix turning into content loss. `lines`/`record_ids` both stay empty, so the 1:1 pairing is untouched. Tests: `tests/test_item72_unattached_gap_marker.py` (9), one of which pins `text == ""` specifically so a future simplification cannot reintroduce the bypass; 5 fail pre-fix |
| 73 | Audio capture follows the Windows default output device, with no picker | **Opus 5** | — | **DETECTION DONE `55fc90c`; still no picker and no auto-switch.** New `alpha/audio/default_endpoint.py` reads the MMDevice default render endpoint ID over ctypes/COM (no new dependency), because **PortAudio cannot answer this at all** — `Pa_Initialize()` snapshots the device list, so `get_default_wasapi_loopback()` returns the start-of-session default forever (a second `PyAudio()` while the first is alive returns in 0.048 ms with an identical index). The endpoint ID is the only usable identifier: the index is a dense concatenation across host APIs so a headset shifts every WASAPI index, and the composed friendly name is not unique (two distinct render endpoints on the dev machine produce the identical string). A 2 s daemon thread compares against the bound device, debounced on two consecutive reads, `""` treated as unknown not changed. Sinks: `jp_accuracy_log` + sticky `signal_label`; **not** a modal, and **not** the transcript publish path — item 67's marker takes that route and is rejected at the identity gate, so it would inherit a dead end. **What it still does NOT do:** appear in `Alpha_output_FINAL.txt`; switch capture to the new device (user must switch back or Stop/Start); detect an unplugged/invalidated device (that raises in the reader loop and kills the reader thread with only a `print`); or detect "right device is default but nothing is routed to it". **Untestable without hardware, and it is the assumption the whole item rests on:** that a real switch changes the endpoint ID, and that the stream starves rather than erroring afterwards |
| 73 | *(original filing, kept)* | — | — | **OPEN, filed 2026-08-16 while scoping item 49.** `alpha/audio/wasapi.py:33` calls `get_default_wasapi_loopback()`; there is no device selection anywhere. The app captures whatever Windows currently has as default output, so on a laptop with a headset, dock or Bluetooth it captures whichever was last selected, and **changing device mid-meeting silently moves the capture source** — no error, no marker, just wrong or silent audio. §1's problem **D** listed "device change" beside network drop, quota and credentials; 44/45/46 covered those three and this one was never picked up. **Independent of item 49's delivery mechanism — it will bite on any machine that is not the development one**, so it is arguably a bigger delivery risk than packaging. Minimum viable fix: detect that the default device changed mid-session and surface it the way item 47's status indicator surfaces connection state; a full picker is more than delivery needs |
| 48 | 60-min stability: bounded queues, ledger memory, Tk rendered-line cap with full history retained | **Opus 5** | 8–9 | **CLOSED 2026-08-14, all four requirements verified.** **Queues** zero at all 197 snapshots of the 99-min run. **Ledger memory** plateaued (+13.5 MB across the last 74 min). **Tk rendered-line cap** already existed and was verified: `MAX_RENDERED_UI_SEGMENTS = 500` with a 100 ms debounce and the store as authoritative history. **Manifest bounded**: 117 MB → **0.263 MB** on run `...20260814-155844`, 354 entries for 71,181 packets (201x), and a local test reconciled every collapsed run's `retained_frame_count` against the WAV frames on disk exactly (7,592,640 per stream x3) with 24/24 chunk sha256 verified |
| 64 | English merge emits a one-token re-send twice (`utterance_lifecycle.py` `_merge_lexical`) | **Opus 5** | — | **DONE 2026-08-12.** `_overlap_join` needs k>=2 and `_tail_resend_splice` needs min_run=3, so a window sliding back by exactly ONE token matched neither and hit the `f"{prev}, {curr}"` branch. Verified against the human reference for run `...095935`: reference "other schools of Muslim", "heretics have attributed", "objectively claiming"; export "schools, schools", "have, have", "Claiming, Claiming" — all three reproduce by calling `_merge_lexical` directly. New `_boundary_token_collapse`, gated on `_audio_spans_overlap` so it only fires where the two spans overlap on the audio clock (a re-send) and never on a continuation ("He said that" + "that was fine" keeps both). Tests `tests/test_english_line_quality.py` |
| 65 | English has no sentence boundary: unreadable wall-of-text lines | **Opus 5** | — | **CLOSED 2026-08-12.** Two halves. (a) Readable grouping into 2-3 sentence lines (`english_line_grouping.py`), wired into `TranscriptStore.get_clean_text` AND `serialize_export_payload`. (b) **The flush is back ON** — its record loss was never the flush's fault. Root cause: every flush commit inherited `is_final: False` from its triggering event and `_display_transcript_item` opens with `if item.get("is_final") is False: return`. The lone survivor on run `...142447` had `is_final: None`, which passes that identity check. Fixed at three layers; flush re-enabled |
| 66 | English commits land mid-sentence, and the provider re-sends the committed tail | **Opus 5** | — | **FIXED 2026-08-12, widened 2026-08-14.** `_strip_committed_tail_prefix` (min_run=3) trims the NEW utterance's head; revises nothing, so the ledger is untouched. Live run `...20260814-101813` showed one survivor: records [4]/[5] shared `"and the number 1 thing"` but were labelled speaker 2 and speaker 1, so the same-speaker gate refused — while their audio spans (131.56-138.24, 136.32-160.48) **overlapped by ~1.9s**, i.e. one span arriving twice. Overlapping audio is now accepted as an ALTERNATIVE to the speaker match (same discriminator as item 64); a genuine speaker change produces no overlap, so problem C's guard is intact |
| 49 | Clean-machine install verification | **Sonnet 5** | 11 | **TODO — but the item presupposes something that does not exist. Read §11 before starting.** There is no packaged build and no packaging tooling in the repo (no `.spec`, no installer script, nothing in `tools/`), so "three full sessions **from the installed build**" (§5, Days 10–11) has no artifact to run from. §11 records the delivery-mechanism options, what was measured about each, and the two facts that decide it |
| 50 | DeepL `context` parameter — previous 2–3 committed lines, unbilled | **Sonnet 5** | 12 | **DONE `3514999`** — status was still "OPTIONAL" until 2026-08-15; the work had already shipped. Both halves are wired: `translation_worker` keeps a `(source_language, text)` tail, builds the hint, and remembers a line only **after** a successful translation, so a line that never reached the provider cannot pollute the next one's context. Guarded twice on capability — `DeepLClient._provider_supports_context()` inspects the installed SDK's signature, and the worker re-checks its own client — because passing an unknown kwarg to an older SDK raises `TypeError`, which the error mapper would classify `retryable=False` and turn every translation into a permanent failure |
| 51 | Fix problem F: English commits self-concatenate on reformatting variants, slid windows and tail re-sends (`utterance_lifecycle.py` `_merge_lexical`) | **Opus 5** | — | **DONE 2026-08-11, confirmed on a second live run.** Gate approved before implementation. Four changes, all inside `_merge_lexical`: edge-punctuation-insensitive token comparison; exact boundary-run join placed *before* the fuzzy gate; order gate measured over the shorter side; bounded tail-resend splice. Both 0.6 thresholds unchanged. Tests `test_utterance_lifecycle_merge_lexical.py` (25). **Measured on the same 364 real input chunks: repeated 4-word phrases 935 → 9, and 8 of those 9 occur in the reference transcript itself (real speech, not merge duplication).** Live export: 17074 → 6288 chars, longest line 5039 → 1580 |

---

## 9. Notes and session log — append only

Record here: baseline numbers from item 40, anything found that is not
being fixed, every rollback, and every place this file turned out to be
wrong.

| Date | Agent / model | What happened |
|---|---|---|
| 2026-08-10 | — | v5 created. Batches 1–3 complete, Batch 4 items 20b/20c/21 complete. Sprint scope set for 2026-08-24 client delivery. |
| 2026-08-10 | Opus 5 | **Item 38 condition 1 — the proposed input filter was wrong.** The design filtered `provider_events.jsonl` on `metadata.source == "system"`. `metadata.source` is not a provenance field at all — it is the **speaker-source label**, written from `source_snapshot.chosen_source / speaker_label` in `deepgram_client.py`. Histogram across all 16 runs that have the file: `system` 199, `utterance_lifecycle_accept_boundary_proposal` 113, `none` 94, *missing* 52, `mic` 3, `mixed` 1. That filter would have discarded 98 genuine ingress rows (`none`/`mic`/`mixed`). The `none` row flagged in the approval is genuine provider ingress — `speech_final: true`, `confidence: 0.998`, real audio timing 98.93–100.85 s — with speaker label "none". Nothing to drop. |
| 2026-08-10 | Opus 5 | **Correct discriminator, verified across 462 rows / 16 runs: `metadata.raw_deepgram_text` presence (equivalently `confidence is not None`).** Perfectly bimodal — 242 rows have both, 220 have neither, **zero mixed**. The 220 are assembler commit re-emissions: `_publish_final_transcript_segment` also calls `record_raw_deepgram_final`, and the Japanese assembler publishes through it carrying lifecycle metadata. `canonical_finalize.py`'s comment claiming provider_events "can never carry a synthetic row" is true only of the `synthetic_record`/`synthetic_lineage` flags — assembler output that is not flagged synthetic does reach the file. |
| 2026-08-10 | Opus 5 | **Coverage is 6 runs, not 27 or 16.** English sessions record **zero** genuine ingress: only `japanese_final_chunk_stabilizer.py` calls `record_raw_deepgram_final` on true ingress, so an en run's provider_events contains re-emissions only (confirmed: all 10 en runs have 0 ingress rows **and** 0 `stable_commits`). Replay from this input is Japanese-only. The 6: `...160529` (25 ingress), `...155922` (3), `...160130` (11), `...134815` (50), `...155334` (71), `...174516` (82). Excluded 11 runs with no provider_events.jsonl: `_pending`, `...132428`, `...160518`, `...160519`, `...160528`, `...134809`, `...155841`, `...173845`, `...192450`, `...192501`, `...192502`. Excluded 10 more that have the file but no ingress (all en): `...132429`, `...132635`, `...150958`, `...153955`, `...160352`, `...133236`, `...155842`, `...173846`, `...192516`, `...134258`. |
| 2026-08-10 | Opus 5 | **§1's "reproduced at higher volume on 08-08 and 08-09" is half wrong.** Measured stable_commits vs ledger records on every run that has an assembler stream: `...160130` 4/3 (−1), `...160529` 10/9 (−1), `...134815` 29/23 (**−6**), `...155334` 32/26 (**−6**), `...155922` 3/3 (0), `...174516` 36/36 (**0**). 08-08 reproduces at volume as claimed. **08-09 does not reproduce at all** — the only 08-09 run with an assembler stream is clean. There is no 08-09 reproduction run to include in the gate. |
| 2026-08-11 | Opus 5 | **Item 38 gate resolved; harness run. Problem A's mechanism is proven and it is not what §1 said it was.** §1 claimed committed sentences "never reach the canonical ledger." They reach it. The assembler commits two **textually disjoint** sentences under one `canonical_utterance_id`; the ledger keys on that id, so the second commit lands as a revision of the first and the first sentence's words are overwritten. Example, run `...160529`, id `jp-utt-19dbf8832ec0`: commit 1 `ですよ。違いますねでやっぱりこっちにいると日本の行事の…` (47 ch), commit 2 `だろう楽しい雰囲気とかも…` (121 ch) — unrelated text, one ledger record, and commit 1 is absent from `Alpha_output_FINAL.txt`. Totals across the 6 replayable runs: **14 dropped sentences, 505 characters, 14/14 independently confirmed absent from the export.** |
| 2026-08-11 | Opus 5 | **The decisions-vs-ledger delta used throughout §1/§5/§7 and the 2026-08-10 rows below is the wrong metric — retracted, not overwritten.** It over-counts (a genuine `revise` writes a second `stable_commits` row for an id that already has its record; that is not a loss) and under-counts (an overwritten id still has a ledger record, so the loss is invisible to it). Corrected per-run drops: `...155922` **1** (delta said 0), `...160130` **2** (said 1), `...160529` **2** (said 1), `...134815` **3** (said 6), `...155334` **6** (said 6), `...174516` **0** (said 0 — the only genuinely clean run). §1's evidence row, §5's Days 1–2 and Days 3–6 gates, and §7 gate 1 all rewritten. This is the third time on this project that a count-based read of evidence files was contradicted by driving the code — same shape as the item 20b retraction in `CANONICAL_KEY_FIELDS_AUDIT.md` §5b. |
| 2026-08-11 | Opus 5 | **The harness's own first verdict was a false pass — recording it because it nearly shipped.** `_unreached_utterances` matched committed utterances to the ledger on `canonical_utterance_id` only, with a comment arguing text comparison would produce false positives. On that measure all 6 runs scored 0 losses and the tool exited 0, reporting "6 replayed, 6 reproduced." The id *did* reach the ledger — carrying the wrong text. An instrument built to find disappearing records was blind to the exact way they disappear. Fixed to condition 2 as approved (match on text **and** id) plus an independent export cross-check; `_dropped_content` now carries that reasoning in its docstring so it is not re-simplified later. |
| 2026-08-11 | Opus 5 | **Decision 5b resolved: the loss does NOT reproduce under fast-feed.** Replay drops 0 of 14. The segmentation diverges outright — identical ingress produces 13 replayed commit decisions against 29 recorded on `...134815`, 19 vs 32 on `...155334`, 18 vs 36 on `...174516` — because no timer fires mid-stream, so the assembler mints a fresh id where the real run reused one, and the collision never occurs. Per condition 4 the harness was **not** adjusted to make the numbers agree. Timing is therefore a determining factor for problem A; opened item **38b** (real `LanguagePipelineWorker` scheduling) rather than retrofitting it into 38. Item 41 does not need 38b — 38's recorded-side measurement already localises the bug to id minting in the assembler. Items 44/48 do. |
| 2026-08-11 | Opus 5 | **English replay coverage — closed, not reopened.** All 10 English runs record zero genuine-ingress rows (only `japanese_final_chunk_stabilizer.py` calls `record_raw_deepgram_final` on true ingress), so replay is Japanese-only by evidence rather than by choice. The `raw_deepgram_finals.jsonl` fallback adapter stays rejected — a second input adapter is two definitions of "replay input", which §0 rule 2 forbids. Logged in **§6** as a candidate if items 44/48 later need English coverage. |
| 2026-08-11 | Opus 5 | **The baseline holds at 7 names; one 8th name is intermittent. Recording the wrong intermediate conclusion too, because it is instructive.** First full-suite run of the session reported `Ran 354 tests … FAILED (failures=7, errors=2, skipped=2)` and this file was briefly edited to say §3's 5+2 baseline was stale. **That was wrong** — the very next full run, `Ran 364 tests … FAILED (failures=5, errors=2, skipped=2)`, produced exactly the 7 documented names. The difference is `test_task9_report.Issue3RealThreadIntegrationTest.test_inactivity_timeout_fallback_survives_immediate_real_stop_5x`, a real-thread timing test: two subtest iterations failed in run 1, none in run 2, and its module passes 9/9 tests in 3 runs out of 3 alone. It is flaky under full-suite load, not a regression, and not caused by this session (nothing in `tests/` imports `tools/replay_run.py`; the only other edits were markdown). **Lesson for future sessions: one deviating full-suite run is not evidence of a broken baseline — re-run before editing §3.** Test count is now 364 (354 + item 38's 10). The flake itself is not fixed here (§0 rule 3); stabilise or quarantine is a human call. |
| 2026-08-11 | Opus 5 | **`ROADMAP_V5.md` did not exist; it was created, then deleted on the human's call. This file is the only plan.** §0 rule 1, §6, §7 and §8 all pointed at a `ROADMAP_V5.md` that was nowhere in the tree. It was created at repo root (commit `2d34a41`), and the human rejected that: the sprint follows `CLIENT_DELIVERY_SPRINT_v5.md` and nothing else. Removed, and every reference repointed to **§6**, which now *is* the post-delivery backlog — the deferrals it already listed, plus the English-replay entry and the WER-gap measure that had been routed to the missing file. Two authorities for "what is deferred" is the same mistake §0 rule 2 forbids for code; one list, in this file. |
| 2026-08-10 | Opus 5 | **Item 38 condition 2 — the host path diverges from production; reporting before writing, as instructed.** Production: `stabilizer.ingest()` → `assembler.ingest()` → assembler posts a *deferred* flush via `LanguagePipelineWorker.schedule_flush(assembler, due_mono, generation, reason)`, executed by a background thread against wall-clock `due_mono`. `JapaneseTestHost` (test_task2c) deliberately bypasses that: its own docstring says timeout scenarios use "the assembler's synchronous `try_execute_continuity_hold` entry point **instead of real timers**", and tests call `assembler.flush(...)` by hand. So *when* a flush happens — which decides what the assembler batches into one commit — is production-timed but harness-manual. Whether problem A survives that change is unknown and is exactly what condition 4 says to report rather than tune around. |
| 2026-08-11 | Opus 5 | **Item 38b done. ~~"Timing is therefore a determining factor for problem A"~~ (2026-08-11 row above) is retracted — it explained the decision-*count* divergence, not the content-loss mechanism, and those turned out to be two different things.** Built `replay_events_real_timer` in `tools/replay_run.py` (`--real-timer`): starts the real `LanguagePipelineWorker` singleton via `start_language_pipeline_worker()`/`stop_and_join()` (the same pair Start/Stop call in production) and sleeps each row's real recorded gap (`metadata.timestamp` deltas) instead of feeding back-to-back, so the deferred flush fires against real wall-clock time. Ran all 6 replayable runs at real speed (span 64–300s each; run in parallel background processes, ~300s wall total instead of the ~956s serial sum). Result, decisions recorded→replayed→(fast-feed for comparison): `...155922` 3→3 (fast-feed 3), `...160130` 4→4 (4), `...160529` 10→10 (8), `...134815` 29→29 (13), `...155334` 32→33 (19), `...174516` 36→35 (18). **Decision counts now match or land within 1 on every run — real timing fully explains the segmentation divergence.** Content loss: recorded→replayed drops `...155922` 1→0, `...160130` 2→0, `...160529` 2→0, `...134815` 3→0, `...155334` 6→0, `...174516` 0→0. **Still 0 of 14 reproduced.** Timing was necessary to explain *how many* commits happen, not *why two disjoint ones share an id*. |
| 2026-08-11 | Opus 5 | **Incidental to building 38b, not a drive-by fix (§0 rule 3) — logged for item 41, not implemented.** Read `japanese_sentence_assembler.py`'s id-mint path end to end while diagnosing why 38b still doesn't reproduce the loss. `update_previous_requested` (line 3635) is computed once, from boundary-stabilizer signals, *before* `decide_stable_revision_action` runs. That call (line 3673) can return `final_revision_action="append"` — a considered verdict that the candidate is a new, disjoint sentence — and when it does, lines 3749–3756 correctly reset `stable_layer_update_previous`/`post_update_previous` and clear `metadata["boundary_should_revise"]`. **`update_previous_requested` itself is never reassigned.** The id-mint gate at line ~3912 (`proposed_action = "revise_previous" if update_previous_requested else "commit_new"`) reads that stale, pre-decision variable — not `final_revision_action`, the function that was specifically hardened by items 10/11/12/19/20c to tell a revision from a new sentence. Cross-checked against real evidence, not left as a code-reading theory: parsed `logs/japanese_accuracy.log`'s `STABLE_REVISION_DECISION` events (pipe-delimited `timestamp \| {json}`, not plain JSONL) on all 6 runs and matched `update_previous_requested == True and final_action == "append"` against the actual id-reuse events in each run's `stable_commits.jsonl`. **12 of 13 id-reuse events match exactly**; the one miss (`...160130`) is plausibly a text-normalisation artifact in the comparison script (a business-glossary correction shifted the text between the two logs by a few characters), not a predicate failure — not chased further, out of this item's scope. Two structural explanations for why real-timer replay still doesn't reproduce this were checked and ruled out: the interim-Deepgram-stream hypothesis (`japanese_boundary_stabilizer.py` has no `interim`/`is_final` reference) and the bypassed-component hypothesis (`get_boundary_stabilizer()` is called from inside the assembler's own `_ingest_locked` chain at line 2299, which replay does exercise). **This is a lead for item 41, not a proof** — unresolved: why the boundary stabilizer disagrees with `decide_stable_revision_action` in the first place, whether the fix belongs at the read site or the write site, and why replay still can't reproduce it despite having the mechanism. Item 41's description in §5 rewritten to start here instead of from scratch. |
| 2026-08-11 | Opus 5 | **Item 39 done, run against all 27 recorded runs.** Gate 1 reuses `replay_run.py`'s own verdict function rather than re-implementing it, so the two tools cannot silently drift apart on what a loss is — confirmed, its per-run counts on the 6 replayable runs are exactly item 38's (1, 2, 2, 3, 6, 0). New findings, from gates that item 38 doesn't run: **gate 2** found an *actual* duplicate line on run `...155922` — `Speaker: 。` (a degenerate near-empty commit) appears twice in `Alpha_output_FINAL.txt`, the only exact-duplicate line across all 27 runs. **Gate 3** found one isolated, credible miss — run `...134815` has 22 of 23 canonical records translated; `jp-utt-5b8f6f0a9300` (`あっ意外とそんなに疲れたっていう感じじゃなくて…`) has zero `translation_jobs.jsonl` rows referencing it at all, not a rejected job, just none submitted. |
| 2026-08-11 | Opus 5 | **Gate 3's English-run failures are flagged UNVERIFIED, on purpose — this is the exact mistake the verification rule in CLAUDE.md exists to prevent, caught before it was asserted as a bug.** Two English runs (`...133236`, `...155842`) score gate 3 as a near-total failure (39 of 44 records, 1 of a smaller set) — but `translation_jobs.jsonl` is **completely empty** for `...133236` (0 rows against 44 canonical commits), not partially populated. A wholesale-empty stream is a different shape from `...134815`'s isolated single miss and reads as either a genuine session-level translation gap on these two runs, or gate 3 checking the wrong evidence file for English sessions (v5 §1 already claims working translation counts elsewhere — "translation jobs 31/31 and 12/12" — so the mechanism evidently works in *some* runs). Not resolved here: whether English sessions are expected to populate `translation_jobs.jsonl` the same way Japanese ones do is unknown to this session and needs a direct answer, not an inference from one field's absence, before gate 3's English-run verdicts are trusted. `score_run.py`'s own output does not silently assert PASS or FAIL past what it measured; this note carries the same caution into the doc. |
| 2026-08-11 | Opus 5 | **Item 40 investigated and blocked — not a scope call, a physical one. Two shortcuts were checked and both are dead ends, not just "not tried."** (1) `Alpha_Benchmark_References/current_japanese_actual.txt` + `current_english_actual.txt` already exist, with a README describing exactly item 40's protocol ("place verbatim spoken references here before scoring... same spoken order as the audio"), and `analyze_alpha_vs_reference.py` already consumes them via `--reference`/`--run-folder`. **Unusable as-is**: both files date 2026-07-24, over two weeks before this sprint's earliest run (`...20260807`); content confirmed mismatched by reading it directly — the Japanese file is a formal リンクブリッジ project-status meeting, none of the 6 replayable runs are (they're casual conversations about sleep habits, Hinamatsuri, dolls); the English file is an unrelated real-sounding internal meeting (names: Alan, Ellen, Phil, Thomas, Neil, Thiago, Bill, Juliana — worth the next session's caution about reproducing this verbatim beyond what scoring needs, it reads as a genuine third-party recording). (2) Checked whether any of the 27 runs' own `audio_temp/{mic,mixed,system}_audio/` captures could be transcribed after the fact instead of a fresh recording. **Dead end**: `AUDIO_TEMP_RETENTION_HOURS: 2` (RUN_MANIFEST) already expired every run's actual audio — `audio_temp_summary.txt` survives (e.g. `...174516`: 36 chunks, 57.8 MB, `expires_at: 2026-08-09T19:38:55`) but the chunk files themselves are gone (`find .../mic_audio/ -type f` → 0, checked on 3 runs). No audio exists anywhere in this repo for anything item 40 could transcribe. **What's ready for whoever runs the live session**: the existing `Alpha_Benchmark_References/` convention and `analyze_alpha_vs_reference.py` are the established tooling — reuse them, do not build a third. `score_run.py --reference` (item 39) already reads the same plain-text format `analyze_alpha_vs_reference.py`'s `_reference_lines_with_hints` expects, confirmed by reading both. Next step is one live 10-min-ja + 10-min-en session per §4's protocol, with the reference hand-written from what was actually spoken (by the human, during or immediately after, while it's still verifiable) — not delegable to this or any agent session. |
| 2026-08-11 | Opus 5 | **The blocker resolved within the hour — human ran both live sessions** (`...20260811-175628` ja, `...20260811-182940` en, both `final_status: completed`) **and supplied a reference the smart way: play a public YouTube video with its own accurate transcript through Alpha, rather than hand-typing a reference live.** Ja source: "Japanese with Yuka" N1 vocabulary lesson. En source: a Mr. Olympia bodybuilding-technique interview. Both are public educational/entertainment content — the privacy caution logged in the row above was about the stale 07-24 reference, which stays archived and unused; it does not apply here. References archived (`Alpha_Benchmark_References/archive_20260724/`) and replaced with the new ones. Japanese scored clean against reference via `analyze_alpha_vs_reference.py`: `cumulative_duplicate_count: 0`, `unaligned_alpha_line_ratio: 0.31` — a usable baseline, item 40 genuinely done for the Japanese half. |
| 2026-08-11 | Opus 5 | **English did not score cleanly — because English output turned out to be badly broken, independent of anything this sprint had looked at. New problem F, see §1.** Reading `Alpha_output_FINAL.txt` for `...182940` directly (not just the scorer's summary numbers) showed every longer utterance as a wall of comma-joined, self-repeating text — e.g. one 5039-character line built from ~112 fragments for what should be a few sentences. Root-caused, not assumed: `utterance_lifecycle.py`'s `_merge_lexical()` (~L91-131) is the function that decides whether two successive texts for the same active utterance are "the same thing, take the newer one" or "two separate adjacent chunks, glue them together." Its containment checks (`curr.startswith(prev)`, `prev in curr`, etc.) require near-literal substring matches. When Deepgram sends a reformatted variant of the same growing hypothesis — smart-format vs verbatim numerals ("50 percent" → "50%"), title casing ("mister" → "Mr.") — none of the containment checks fire, and it falls to the last-resort branch meant for genuinely separate phrases, gluing them with a comma. Confirmed against the real function, not by reading alone: `_merge_lexical("So I'm mister Olympia.", "So I'm Mr. Olympia,")` returns `"So I'm mister Olympia. So I'm Mr. Olympia,"` — concatenated, using text pulled directly from the corrupted output. `_apply_active_update_locked()` (~L1439-1544) compounds it: it correctly classifies some of these as `REPLACE_ACTIVE` (its own logic recognizes intent to replace, not merge) but writes `active.text = merged` unconditionally at L1544 regardless of which decision was chosen, so even the correctly-classified cases still get the concatenated text. Every English commit goes through this path — `should_use_utterance_lifecycle()` returns true for any non-Japanese session — so it reaches the canonical ledger, the translation input (explaining the garbled Japanese translation the human also supplied), and the client-facing export identically. |
| 2026-08-11 | Opus 5 | **Confirmed pre-existing, not introduced by anything this sprint touched, and confirmed Japanese-unaffected.** Run `...20260808-133236` (English, already read once for item 39's gate 3) shows the identical pattern — this has been silently corrupting every English session's output the whole time; nothing in this sprint's tooling had looked at English output *content* before today (`replay_run.py`/`score_run.py` gate 1 both skip English entirely — zero genuine ingress in `provider_events.jsonl` by design — so neither ever read an English export closely). Japanese confirmed clean by direct reading of `...175628`'s export and because Japanese never reaches this function (`should_use_japanese_final_stabilizer` routes it to `japanese_sentence_assembler.py` instead). Severity, measured directly rather than trusting `analyze_alpha_vs_reference.py`'s summary (its `cumulative_duplicate_count`/`alpha_output_cumulative_duplicate_suspected` read `0`/`False` on this run despite the corruption — its detector is tuned for a different duplication shape and has a blind spot here, worth someone's attention separately): on `...182940`, 5 of 54 export lines are over 400 characters, and those 5 lines carry **85.9% of the export's total characters**. A client reading this transcript sees mostly unreadable repeated text, not occasional glitches. Not fixed — outside this item's scope and this is a shared function underlying every English commit, exactly the kind of change that needs the gate process; opened as item 51, `[gate]`, recommended (not decided) to jump ahead of items 41-43 given it's more severe than problem A for any English-source client use. |
| 2026-08-11 | Opus 5 | **Item 51 — half of the previous row's root cause is RETRACTED before it misled anyone.** That row said `_apply_active_update_locked()` "correctly classifies some of these as `REPLACE_ACTIVE` but writes the concatenated text regardless", implying two independent defects. **It does not, and there are not two.** That function *derives* its decision from `_merge_lexical`'s return value — `if _norm_text(merged) != curr_n and != prev_n: decision = EXTEND_ACTIVE`. Driving the real function on both known corruption pairs returns `EXTEND_ACTIVE`, not `REPLACE_ACTIVE`, so `active.text = merged` is *consistent* with the decision rather than contradicting it. One root cause, entirely inside `_merge_lexical`; fixing it corrects the decision label as a side effect and `_apply_active_update_locked` needed no edit. Same failure shape as the item 20b retraction — a plausible read of the code that the code itself contradicts once driven. |
| 2026-08-11 | Opus 5 | **Item 51 done. Worked from the real recorded input, not from the UI transcript.** English records no genuine provider ingress (item 38's finding), so `provider_events.jsonl` holds only the *already-merged* corrupted text — useless as an input fixture. Recovered the true 9-chunk input sequence for run `...182940` utterance `U-1` by decomposing the recorded corrupted line and **verifying the reconstruction by re-merging it through the pre-fix function and getting the recorded 472-character line back byte-for-byte.** That sequence exposed **two** failure modes, not one: **(a)** reformatted re-sends of the same span (`"So I'm mister Olympia."` / `"So I'm Mr. Olympia,"`) — the word-overlap gate that exists to catch these compared tokens with punctuation glued on (`_norm_text` only lowercases), so `"olympia."` ≠ `"olympia,"` and the score fell under 0.6; and **(b)** slid windows repeating the previous chunk's tail (`"…the best bodybuilder in"` + `"bodybuilder in the world…"`), which the concatenation branch duplicated despite its own comment claiming it handles "non-overlapping lexical spans". Fixing only (a) leaves (b) visible, so both were in scope — same defect class, same function, not scope creep. |
| 2026-08-11 | Opus 5 | **Item 51 — the fix nearly introduced silent content loss, caught by driving the real sequence rather than trusting the unit pairs.** First working version put the new boundary-run join *after* the similarity gate. On the real sequence that scored a slid window carrying new tail content as "same utterance re-said", and the gate returns whichever side is *longer* — which is the accumulator, because it still holds the earlier text — so `"lifting weights. Come on."` was silently dropped. Output looked clean (101 chars, no duplication) and was **wrong**. Reordered so the exact boundary-run check runs first: literal evidence outranks a fuzzy score. Pinned by `test_slid_window_with_new_tail_keeps_the_tail`. Silent loss is strictly worse than visible duplication, and the only reason it surfaced is that the end-to-end fixture is real recorded input with a known-correct answer. Third change, also needed: the order gate used `SequenceMatcher.ratio()`, which is symmetric and so penalises length difference — a 4-token chunk against the 8-token growing version of itself scored 0.50 and was rejected despite 3 of its 4 tokens matching in order. Switched to in-order matched tokens over the *shorter* side, the same asymmetric convention the set-overlap gate immediately above it already used; the 0.6 threshold itself is untouched. |
| 2026-08-11 | Opus 5 | **Item 51 verification, and what is still not verified.** Method step 5 done properly: the 20 new tests were run against pre-fix code — 8 of 8 bug-detecting assertions failed, reproducing the recorded corruption exactly (472 chars, "olympia" ×4, "bodybuilder" ×6), while the disjoint-chunk negative control passed *both* before and after, proving that control is not the fix doing the work. Post-fix the verified real sequence folds to **127 characters, each phrase once, matching the reference transcript**. Full suite **405 tests, 5 failures + 2 errors + 2 skipped, the same 7 names** — no regression, and `test_bugfix_spec_regression.py`'s existing concatenation expectation still passes untouched. **Not yet verified: the export-wide number.** The 85.9%-of-characters figure came from a live run, and re-measuring it needs one more live English session — this session cannot run the app. That is the one open confirmation on item 51; everything measurable without the app is measured. |
| 2026-08-11 | Opus 5 | **The human ran the confirming live English session (`...20260811-212531`) — the fix held on the first utterance and exposed a third shape of problem F that the first three changes did not reach.** Export-wide: 17074 → 6288 characters, longest line 5039 → 1580, and the first utterance came out clean and matching the reference. But 5 lines were still over 400 characters. Cause is a **tail re-send**: once an utterance has accumulated, Deepgram re-sends only its most recent span, revised and extended — `"…You lower it. So even though you failed, positively, we're"` followed by `"So even though you failed positively, we can do a couple of extra reps"`. Whole-against-whole comparison cannot see it (the shared run is a small fraction of a long accumulator, so the similarity gate scores 0.46) and `_overlap_join` cannot either (the run is not a clean suffix of `prev` — `prev` ends in a partial `"we're"` that `curr` revises away). Both fell through to concatenation. Added `_tail_resend_splice`: find the longest run of `curr`'s leading tokens occurring contiguously in `prev`, latest occurrence first, and keep everything before it. Placed **after** the similarity gate, because it is the only step in this function that can discard text. |
| 2026-08-11 | Opus 5 | **Item 51 final measurement — like-for-like, both versions folding the same 364 real recorded input chunks from run `...212531`.** Original pre-fix code: 13401 characters, **935** repeated 4-word phrases, 1.29× the reference length. After all four changes: 4589 characters, **9** repeated phrases, 0.44× the reference. **8 of those 9 remaining repeats appear in the reference transcript itself** — the speaker genuinely said them — leaving one artifact that is a Deepgram mis-transcription (`"just showed the stress"`), not a merge defect. The splice's safety property is that it can never discard more than `max_orphan` (4) tokens regardless of accumulator length, which is what bounds the risk of trading duplication for silent loss; `test_splice_can_never_discard_more_than_the_orphan_bound` pins it. Suite **410 tests, 5 failures + 2 errors + 2 skipped, same 7 names**. Problem F is closed. |
| 2026-08-11 | Opus 5 | **Item 41 — investigated, and the honest verdict is NOT PROVEN. Full write-up in `PROBLEM_A_ROOT_CAUSE.md`** (repo root, because `troubleshooting/` is gitignored — the item 41 prompt's suggested path could not be committed). Phase 1: 10/10/9 reproduces, but **zero ids are in `stable_commits` and absent from the ledger** — the "missing record" is a *duplicate id*, not a missing one. Phase 2 established an exceptionless rule across all 6 runs: a sentence is lost **iff** its `canonical_utterance_id` is carried by more than one stable row **and** those rows' texts are disjoint rather than nested; the earliest row is the one lost. Phase 4's strongest evidence is a natural control group — of the 14 id-reuse events, the 9 with disjoint text all had `update_previous_requested=True`, the 3 with nested text all had it `False`, and `decide_stable_revision_action` returned `final_action="append"` (reason `speaker_boundary_forced_new_line`) in **every single case**. The revision authority always got it right; the id-mint gate ignored it. |
| 2026-08-11 | Opus 5 | **Item 41 — correction to item 38's own measurement: the true loss count is 10, not 14.** `tools/replay_run.py`'s `_dropped_content` normalises whitespace only, so it cannot see two legitimate rewrites and counted both as losses: (1) a cleanup step stripping a leading `。` (`jp-utt-f8bf3bbf9fb2`, `...160529`), and (2) a business-accuracy correction inserting `て` (`jp-utt-453c4fd9c80f`, `...155334`). Both texts **are** in the export; confirmed by reading it directly. Per-run true losses: `...155922` 0, `...160130` 1, `...160529` 1, `...134815` 3, `...155334` 5, `...174516` 0. **Not fixed — repairing the detector means editing a `.py`, which item 41 forbids.** Any future "problem A is fixed" claim measured with that tool will be wrong by ~2 in 12. |
| 2026-08-11 | Opus 5 | **Item 41 — Phase 5 FAILED, and that is the reportable outcome, not something to paper over.** Driving the real assembler headlessly with the two real disjoint sentences from `...160529`: same speaker reuses the id but **loses nothing** (the ledger retains both); a speaker change mints **two distinct ids** and also loses nothing. Neither reproduces the recorded combination of speaker-boundary decision **and** id reuse **and** loss. **This is the second independent harness to fail** — item 38b's real-timer replay also reproduced 0 of the losses. The mechanism depends on live state neither harness reconstructs (candidates: boundary-stabilizer internal state, `_last_stable_commit` lineage, when `_current_canonical_utterance_id` is reset). **Consequence: item 42 cannot satisfy §4 step 5 yet — there is nothing to make fail.** Closing the gap needs one instrumented live Japanese session logging `update_previous_requested` and `proposed_action` at the gate; that is a `.py` edit and needs human approval first. |
| 2026-08-11 | Opus 5 | **Item 41 — found and not fixed (§0 rule 3).** (a) `stage_manifest.json` and `export_coverage_report.json` report the **same** metric name `export_coverage_ratio` as **1.0** and **0.9** for run `...160529`. (b) The `coverage_ratio` gate passes at 1.0 on that run while four sibling fields *in the same file* — `source_commit_coverage_ratio`, `lineage_coverage_ratio`, `text_coverage_ratio`, `export_coverage_ratio` — all read **0.9**, i.e. 9 of 10, precisely the lost commit. The evidence of problem A was recorded next to the gate that declared the run clean. (c) Item 20b's `revision_target_id` guard is keyed off `final_revision_action` *after* that variable is overwritten by a value derived from the stale gate, so 20b's fix is silently undermined on this path — item 42 must re-verify 20b's regression test still means what it claims. |

| 2026-08-12 | Opus 5 | **Item 41 closed — root cause PROVEN, upgrading the 2026-08-11 "strongly indicated, not proven" verdict.** That verdict was honest when written: Phase 5 had genuinely failed. Two things closed it, neither requiring the live-session instrumentation that was proposed and is now **obsolete**. **(1) Wrong entry point.** The failed attempt drove `_route_stable_publish`, which merges a short follow-up into the held buffer *before* publishing, so the revise it produced was non-destructive — the new text contained the old, which is exactly the harmless class that accounts for 3 of the recorded revises losing nothing. Driving `_publish_sentence` — one layer lower, where the defect lives, with every function below it still production code including the real ledger — reproduces the loss immediately. **(2) The missing structural fact.** `previous_record` is built with `line_id`/`text`/`source_raw_event_ids`/`start_time`/`end_time`/`utterance_id`/`segment_id` and **no `speaker`**, though `_last_stable_commit` carries one. `previous_speaker` therefore reads `None`, `speakers_confirmed_same` is fail-closed on `None`, and that guard sits before every other rule — so `decide_stable_revision_action` returns `append` on every non-first commit (**105 of 111** measured across the corpus). **Rules A–F, `REVISION_CONTENT_LOSS_GUARD_ENABLED`, `REVISION_TERMINAL_SENTENCE_GUARD_ENABLED` and `_unique_content_lost` have never executed on the Japanese path.** The engine always says append; the stale flag always overrides it; the loss is systematic, not occasional. |
| 2026-08-12 | Opus 5 | **Item 41 Phase 5 fixture: `Alpha_Live_Translator/tools/reproduce_problem_a.py`.** Controlled, single independent variable — `boundary_should_revise` absent gives **2 ledger records, 2 distinct ids, both sentences intact**; set to `True` gives **1 record, 1 id, `applied_action: revise`, and the first sentence destroyed**. Nothing else differs. The control is asserted too: the fixture exits `2` with `FIXTURE INVALID` if the control ever stops being clean, so a loss can never be blamed on the harness. Deliberately **not** under `tests/` — it is designed to fail while the bug exists, and putting it in the discovered suite would move the 410 / 5F + 2E + 2S baseline. It exits **1** today, for the proven reason, which is sprint §4 step 5 already satisfied for item 42 before that item starts. Baseline re-confirmed unchanged after adding it. |
| 2026-08-12 | Opus 5 | **Found while proving item 41, logged not fixed (§0 rule 3) — a trap for item 42.** Adding the missing `speaker` key looks like the clean fix and is the most dangerous of the three options. It would let `speakers_confirmed_same` return `True` for the first time in this corpus, making **Rules A–F reachable for the first time** — a large block of never-exercised decision logic switched on during a data-loss fix, which is precisely how this ledger already records six cases of one fix unmasking the next. There is also a known hole waiting behind it: `_unique_content_lost` returns `False` whenever `len(prev_n) < 8`, so `_content_loss_risk` clears and a **short** previous record can be legally overwritten by disjoint text under Rule C — converting a systematic loss into an intermittent one that is harder to detect. If that option is taken it must be its own separately-tested item, not folded into 42. Full item 42 brief: `PROBLEM_A_ROOT_CAUSE.md` §8. |
| 2026-08-12 | Opus 5 | **Item 41's falsification workflow: 1 of 8 agents survived.** The trace agent completed; all six falsification agents and the adjudicator died on a session limit. Its trace was treated as a **lead, not a finding** — every load-bearing claim above was re-verified in-session against the source before being written down (the missing `speaker` key, the fail-closed guard, the guard's position ahead of all rules, and the destructive `target["final_text"] = text` in `_revise_record_unlocked`). Phase 4's candidate verdicts in the proof doc §4 were reached independently of the workflow, from direct evidence, and the workflow did not contradict them. |

| 2026-08-12 | Opus 5 | **Item 42 done — problem A fixed, and the obvious fix was NOT the right one.** Pointing the id-mint gate at `final_revision_action` (the revision authority's verdict) is the fix item 41 §5 recommended, and driving it proved it **insufficient on its own**: because `previous_record` carries no `speaker` key the authority is fail-closed to `append` on every non-first commit, so that gate would be *always* `commit_new` — which removes the data loss by converting every genuine extension into a second, near-duplicate line. That is problem F's failure mode on the Japanese side. Measured before writing the fix, with a two-property harness driving the real `_publish_sentence`: **P1 (no loss) FAILED and P2 (no duplication) PASSED** pre-fix, so a naive fix would simply have swapped which property was broken. |
| 2026-08-12 | Opus 5 | **The fix: the revise must be non-destructive by content.** New `_revision_is_non_destructive(previous_text, candidate_text)` — a revise is allowed only when the candidate still contains the previously committed text (whitespace-insensitive; punctuation deliberately **not** stripped, because `。`/`、` carry sentence-boundary meaning and ignoring them would let a genuine rewrite pass as an extension). This is exactly the discriminator item 41 measured as exceptionless across the corpus: lost **iff** the id is reused **and** the texts are mutually disjoint. A blocked revise now logs `DESTRUCTIVE_REVISE_BLOCKED_NEW_UTTERANCE_ID` with both texts and mints a fresh id — the sprint's "lands in the ledger or fails loudly, never silently dropped" requirement, satisfied without an architecture change. Post-fix both properties pass; `tools/reproduce_problem_a.py` flipped 1 → 0. |
| 2026-08-12 | Opus 5 | **§0 rule 2 (never two authorities) — how it was satisfied.** Item 41 §5.1 warned that fixing the gate's *read* without addressing the later `final_revision_action = … if proposed_action == …` overwrite would leave two disagreeing notions of "was this a revision". After this change the gate is the **only** place the Japanese path decides append-vs-revise, and that later line is now just a relabel of that single decision — it cannot disagree with the gate, whereas before it claimed to carry the authority's verdict while the gate was driven by a stale pre-decision flag. The line is kept (item 20b's `revision_target_id` guard consumes it) and carries a comment saying exactly that. Item 20b's own regression test still passes untouched. |
| 2026-08-12 | Opus 5 | **Method step 5 done properly, and what the evidence does and does not cover.** The 11 new tests were run against reverted code: the two loss tests failed with the exact recorded signature (first sentence destroyed, 1 record where 2 were expected) while `GenuineRevisionStillWorksTest` **passed on both sides**, proving that half was pre-existing behaviour the fix preserves rather than something the fix invented. Suite **421 tests, 5 failures + 2 errors + 2 skipped, the same 7 names**. **`tools/replay_run.py` cannot certify this fix** — its replayed side was already 0 before the change (item 38b), so it does not discriminate; the fixture and the regression tests are the evidence. Also surfaced while re-measuring: the 2026-08-11 Japanese live run `...20260811-175628` shows **6 recorded drops**, confirming problem A was still occurring live right up to this fix (subject to `_dropped_content`'s known ~2-in-12 over-report). |

| 2026-08-12 | Opus 5 | **Item 43 done — and problem B's headline number was wrong.** §1 said "2 of 2 quarantine events ever recorded were misclassified real Japanese speech". Re-measured across every recorded run: **9 quarantine events, 8 drops (4 distinct fragments), 1 release.** The distinct dropped texts are `寝れた、幸せ、` ("slept, happy"), `。忘れちゃうし、` ("and I forget"), `最近また` ("recently again") — all real speech — and `、`, a bare comma, which is genuine noise. So **3 of 4, not 2 of 2**. §1 row B corrected. The direction of the original claim holds and item 34's supersession stands: 4 observations is still far too small to tune a noise threshold on, which is exactly why item 43 makes the path non-destructive instead of trying to classify better. |
| 2026-08-12 | Opus 5 | **Item 43's real constraint was the lock, not the policy.** `_drop_expired_quarantine_locked` is invoked by `language_pipeline_worker._run_quarantine_drop` **with the assembler lock already held**, and the commit path re-acquires it — so committing inline would deadlock. Expiry therefore queues to `_quarantine_recovery_pending` and `_drain_quarantine_recovery` commits outside the lock, the same collect-inside/commit-outside shape `flush_quarantine_on_stop` already used. The drain is hooked at `_ingest_safe`'s entry (the first unlocked point after the worker's timer) and at stop, and is re-entrancy-guarded because it calls back into `_ingest_safe`. Stop also drains first, so a fragment that expired seconds before Stop is not stranded in the queue. |
| 2026-08-12 | Opus 5 | **A hazard found while implementing item 43, and the one deliberate exception to "never drop".** Committing text with no word characters would fail `accept_boundary_proposal` with `"empty_text"`, and the assembler converts **any** proposal failure into `_assembler_commit_gate_failed`, which is cleared only in `reset()` — so every remaining commit in the session is silently dropped. Feeding the corpus's bare `、` into the commit path would therefore have traded a one-fragment loss for a whole-session loss. Fragments with no word characters are consequently logged `NOISE_FRAGMENT_NOT_COMMITTABLE` rather than committed — explicit and distinct from the old silent `NOISE_FRAGMENT_DROPPED`, so it stays visible in evidence. `test_punctuation_only_does_not_trip_the_session_killing_commit_gate` pins it. |
| 2026-08-12 | Opus 5 | **Item 43 — what recovery does and does not reach, stated honestly.** Verified on the realistic path (quarantine expires mid-session, more speech follows): the recovered fragment reaches the canonical ledger, merged into the following sentence. Verified on the stop path: the fragment now leaves quarantine into the commit path instead of vanishing. **But a recovered fragment that is an incomplete tail at Stop is then suppressed by the pre-existing stop-tail path** (`suppress_early` → `STOP_TAIL_CANDIDATE_SUPPRESSED`), so it still will not appear in that one case. That is a separate, documented behaviour outside item 43's scope — logged here rather than fixed, per §0 rule 3. Method step 5 done per site: reverting the expiry recovery failed 3 tests, reverting the stop-flush recovery failed 1, each with the original signature. Suite **429 tests, 5F + 2E + 2S, same 7 names**. |

| 2026-08-12 | Opus 5 | **Items 22, 23 and 33s done — problem C closed.** Both defects were reproduced against the real `UtteranceLifecycleOwner` before any code changed. Item 22: two speakers with **no identified label** produced `'the first speaker said this, a totally different remark'` as a single line, because `_compatible_with_active_locked` compared `int(active.speaker or 1) != int(speaker or 1)` and every unknown coerced to `1`. Item 23: a genuine speaker change produced `'speaker one is talking here, speaker two interrupts now'` as a single line, because Case B forced a new utterance **only when the active one was already committed** — and a held final chunk is uncommitted by definition. Case C had always gated correctly; Case B is now aligned to it. |
| 2026-08-12 | Opus 5 | **22 and 23 interlock — neither is sufficient alone, which the revert proof surfaced.** Reverting item 22 alone failed 3 tests; reverting item 23 alone failed **4** — the same 3 plus its own. Item 22 is what makes `same_active` False for an unknown speaker; item 23 is what makes Case B actually act on `same_active`. With either reverted the unknown-speaker turns merge again. Worth knowing before anyone reverts one of them in isolation believing it is independent. |
| 2026-08-12 | Opus 5 | **`speakers_confirmed_same` alone would not have fixed item 22.** The ledger's wording is "use `speakers_confirmed_same()`", but that primitive is fail-closed only on `None` — it reads `0 == 0` as a *confirmed* match, and this codebase uses `0`, `None` and `""` interchangeably for "no speaker". So `_known_speaker` normalises every unknown form to `None` first, and that normalisation is load-bearing, not tidying: `test_speaker_zero_is_treated_as_unknown_not_as_a_match` fails without it. |
| 2026-08-12 | Opus 5 | **Item 33s is a latent-trap removal, not an observable fix — stated plainly so nobody claims a behaviour change that is not there.** `_resolve_output_speaker` stabilises the speaker *for display* when evidence is weak, and one of its own lock reasons is `_looks_like_speaker_continuation_tail` — "this text reads like a continuation". Feeding its output into `decide_stable_revision_action` closed a loop where the relabel manufactured the same-speaker agreement the guard exists to test. `_publish_sentence` now captures the raw provider label as `boundary_speaker` before the relabel and passes that instead. **This changes nothing today**, because item 41 proved the guard is already fail-closed (`previous_record` carries no `speaker` key), so the comparison never reaches a match either way. It matters the moment anyone adds that key — which item 42's notes already flag as the dangerous option. |
| 2026-08-12 | Opus 5 | **Anti-fragmentation checked, not assumed.** These guards make merging strictly harder, and over-tightening would trade a cross-speaker merge for a transcript chopped into fragments — a different visible-on-stage defect. `SameSpeakerStillMergesTest` pins that one speaker's continuous speech still merges across 3 chunks, and `tools/replay_run.py --all` shows replayed decision counts on all 7 runs **identical** to before the change (3/4/8/13/19/18/57). Problem A's fixture still exits 0. Suite **442 tests, 5F + 2E + 2S, same 7 names**. |

| 2026-08-12 | Opus 5 | **Items 44 and 45 — most of both was already built, so only the missing pieces were touched.** Investigated before writing anything: Deepgram reconnect already had exponential backoff (`_dg_backoff_seconds`, capped, reset on first post-reconnect transcript), an audio replay buffer (`_dg_replay_buffer`, snapshotted before reconnect), single-flight locking (`_dg_reconnect_lock`) and auto-reconnect while listening — all shipped earlier as "fix 5". DeepL already had error classification (`retryable` per code: `http_429`/`temporary_server` yes, `quota_exceeded`/`auth_failed` no) and retry-with-backoff in `_translate_job`. Rebuilding any of that would have been an architecture change (§0 rule 1) and risked two authorities on the same path (§0 rule 2), so it was verified and left alone. |
| 2026-08-12 | Opus 5 | **Item 44's actual gap: the gap.** Replay restores audio that was *queued* during the outage, but audio captured while the socket was down is simply gone — so the transcript stitched across the hole and read as continuous speech, and a client could not tell a clean recording from one with a hole in it. `_mark_deepgram_gap_if_any` emits a marker carrying the real outage duration, flagged `synthetic_record` and `translation_eligible: False` so it is never sent to DeepL or counted as an utterance. Two deliberate details: the clock starts only on an **unexpected** close (a user Stop would otherwise put "connection lost" at the end of every normal session), and the whole thing is wrapped best-effort — losing the annotation must never break the reconnect that restores transcription. Sub-2s blips are not marked; the marker would be noisier than the hole. |
| 2026-08-12 | Opus 5 | **Item 45's circuit breaker is deliberately separate from the existing quota disable.** Quota is permanent for the session and stops accepting work; this is for a *transient* outage, where every segment otherwise spent the full retry ladder (~7s) before failing. It opens after 3 **consecutive** failures — consecutive, not cumulative, so an intermittent failure cannot creep it open across a long healthy session — and one success closes it. Re-opening during a continuing outage doubles the cooldown from 30s to a 300s cap, so a long outage stops hammering the provider, but the cooldown **always expires**, so recovery is never refused (pinned by `test_the_circuit_always_expires_so_recovery_is_never_refused`). When open it skips the provider call but the job still flows through the normal result/evidence path, so counters, latency and the events file stay consistent with every other outcome. Quota is excluded from the breaker's count on purpose — double-counting it would open a circuit nobody consults. |
| 2026-08-12 | Opus 5 | **What is NOT verified, stated plainly.** Item 44's sprint gate is *"60-minute session with a deliberate network drop ends `completed` with zero content loss"*. That cannot be checked from recorded runs or unit tests — it needs a human at the machine with the app running, dropping the network mid-session. **Item 44's code is done; its gate is not signed off**, and the ledger row says so. The same applies to seeing the marker render in the UI. Item 45's "never block the transcript" holds structurally — translation runs on its own worker thread and the transcript path only enqueues — and the breaker makes that strictly better by removing the retry ladder from the failure path, but an end-to-end DeepL outage during a live session has also not been exercised. Suite **456 tests, 5F + 2E + 2S, same 7 names**. |

| 2026-08-12 | Opus 5 | **Correction — the local-only audio-retention change was swept into commit `fd0330e` unintentionally.** `TEMP_AUDIO_RETENTION_ENABLED` and `TEMP_AUDIO_AUTO_DELETE_ENABLED` were set `False` on 2026-08-11 at the human's request (so live-test audio survives the 2h auto-delete) and had been deliberately left **uncommitted** ever since. Items 44/45 also edited `alpha/constants.py` (to add `DG_GAP_MARKER_*` and `TRANSLATION_CIRCUIT_*`), and staging that file carried the retention flags along with it. Not amended — this project's rule is that history stays honest rather than being rewritten — so it is recorded here instead. **Both flags are now `False` on `origin/main`.** The human said they would re-enable them later; whoever does that should set both back to `True` in one commit. Nothing else in that commit is affected. |

| 2026-08-12 | Opus 5 | **Items 46 and 47 done, as pure functions in `alpha/utils/service_status.py`.** Both are decision rules, and putting them in the UI would have made them untestable here (`SKIP_TK_INTEGRATION_TESTS` is set for the whole suite) and would have let each call site re-derive "what state are we in". Credential *status* helpers already existed in `alpha/config.py` (`get_deepgram_key_status`, `has_deepl_api_key`) and were reused rather than duplicated — what was missing was turning them into an actionable message and a single connection state. |
| 2026-08-12 | Opus 5 | **Item 46's real decision is which failures may block Start.** Deepgram blocks: without a key there is no transcript, so starting would produce an empty session. DeepL does **not** block: a transcript-only session is degraded rather than broken, and refusing to start over a missing translation key would be a worse outcome than running without it. Missing and placeholder keys are separate codes because they need different advice — "set a key" versus "you left the example in". The runtime half is covered too: a key that was valid at Start and is later revoked surfaces as `failed` with advice, rather than an endless reconnect loop that looks like a network problem. |
| 2026-08-12 | Opus 5 | **Item 47's severity ordering deliberately differs from the row's wording, and a test caught me getting it wrong first.** The ledger lists the states "connected / reconnecting / degraded / failed", and I initially encoded that order literally, ranking `degraded` above `reconnecting`. `test_reconnecting_outranks_translation_degraded` failed, correctly: `degraded` means translation is failing while the transcript keeps flowing, whereas `reconnecting` means the transcript itself has stopped and audio is being lost. Losing the words is worse than losing the translation of them, so the final order is connected < degraded < reconnecting < failed. The states are unchanged; only their ranking is, and the module says why in a comment so it is not "tidied" back. |
| 2026-08-12 | Opus 5 | **Not verified for 46/47: the UI rendering.** These return the state and the sentence; nothing in this session confirms the Tk indicator actually displays them, because the suite runs with Tk integration skipped. Wiring plus a visual check belongs to the Days 10-11 live validation, alongside item 44's network-drop gate. Suite **473 tests, 5F + 2E + 2S, same 7 names**. |

| 2026-08-12 | Opus 5 | **Audited the v4 roadmap's leftovers at the human's request; 24 and 26 were worth fixing, 25 was not.** Of `BUG_FIX_ROADMAP.md`'s 20 pending items, the sprint had already closed 5 (22, 23 directly; 28→41, 29→42, 34→43) and partly closed 33 (only `33s`). Item **24** turned out to be the third sibling of 22/23 — `revise_last_transcript_snapshot` accepted a `speaker` argument and never compared it — and **it was orphaned in this file**: §8 marked it `SPRINT — day 6` while §5's day plan never listed it, so following §2's instruction to work from §5 would have skipped it entirely. Fixed, and the §8 row now says so. |
| 2026-08-12 | Opus 5 | **Item 26 is the half of problem A that item 42 did not cover.** Item 42 stopped the *assembler* proposing a destructive revise. The ledger itself still overwrote `final_text` with no `source_version` comparison, so any other caller — English path, UI path, a future one — could repeat the loss that cost 10 sentences. The guard added is deliberately about **ordering** ("is this write older than what the record already holds?") and not about content: item 42 owns the non-destructive question at the caller, and re-implementing that rule inside the ledger would put two authorities on one decision, which §0 rule 2 forbids. Equal versions still pass (ordinary idempotent retries) and callers that supply no version are unaffected — both pinned by tests. |
| 2026-08-12 | Opus 5 | **Item 25 investigated and deliberately left deferred, with the reasoning recorded so it is not re-litigated.** The UI does mint its own ids (`jpm-utt-{uuid4}`, three sites in `main_window.py`). Three things make it a non-blocker: it is a **separate namespace** from the assembler's `jp-utt-`, so it cannot collide with the ids item 42's gate manages; it is **Japanese manual mode only**, not the live-session path a client demo uses; and the code carries a comment showing the current behaviour was itself a deliberate fix (carry real identity rather than leaving it unset). Relocating identity minting is the kind of change §0 rule 2 wants done carefully rather than at the end of a sprint. Suite **481 tests, 5F + 2E + 2S, same 7 names**. |

| 2026-08-12 | Opus 5 | **New batch: items 60-63, silent-failure landmines. All four verified against the code before being fixed — none came from the audit workflow, which returned zero findings (all 7 agents died on a session limit).** **60**: `_assembler_commit_gate_failed` was set at three sites and cleared at exactly one, inside `reset()` — so a single failed proposal silently discarded **every remaining commit of the session** while the run still reported `completed`. **63** was worse in kind: `_assembler_exception_recovery_buffer` was **written and never read** — grep showed an init, a reset, one write and zero reads, so text stashed there on an assembler exception was guaranteed lost; the name promised a recovery no code performed. **62**: the Japanese path discarded `_display_transcript_item`'s return, so a `retry_pending` verdict never reached its handler and the item was dropped instead of retried. **61** is visibility rather than loss — a disabled ledger returned `ok=True` with no `record_id` (sound) but nothing anywhere announced that the store feeding the FINAL export was off. |
| 2026-08-12 | Opus 5 | **How 60 and 63 were fixed, and what was deliberately NOT done.** The commit gate was **kept**, not deleted — refusing to keep committing into a transaction that has already broken is correct. What changed is its *scope*: the failure belongs to the utterance that broke, so the gate now records `_commit_gate_failed_utterance_id` and clears itself when the utterance changes, the same degrade-then-recover shape as item 45's breaker. Deleting the gate would have traded a silent-loss bug for an integrity bug. Item 63 reuses **item 43's existing drain** (`_quarantine_recovery_pending` → `_drain_quarantine_recovery`) rather than adding a second recovery mechanism, which would have been two authorities on one job (§0 rule 2); a test pins that the drain stays the single path. |
| 2026-08-12 | Opus 5 | **A suspected regression was investigated and cleared — recording it because the instinct to blame my own change was wrong.** After these fixes the full suite showed an 8th failing name, `test_task9_report…test_inactivity_timeout_fallback_survives_immediate_real_stop_5x`, and it then failed **2 of 3 runs in isolation** where §3 documents it passing 3/3. Runtime had also collapsed from ~80s to ~4s, which looked like a code path being short-circuited. Reverting item 62 alone did not restore it; stashing **all** of items 60-63 did not restore it either — a clean tree failed identically, 2 of 3. So it is the documented load-flake, just flakier under the heavy parallel workflows this machine has been running, and **not** a regression. §3's "re-run before concluding" rule is what caught this; without it the correct fixes would have been reverted. Suite **489 tests, 5F + 2E + 2S, same 7 names**. |

| 2026-08-12 | Opus 5 | **Item 44 REOPENED — my earlier "reconnect already works" claim is retracted.** I verified that fix 5's machinery (backoff, `_dg_replay_buffer`, `_dg_reconnect_lock`, auto-reconnect) *existed in the source* and concluded only the gap marker was missing. I never verified it *executes*. A human live network-drop test settled it: WiFi dropped 10:20:54, reconnected 10:21:30, and the app never resumed — 5 more minutes of audio produced nothing. Logs for run `...20260812-095935` show **411 `AUDIO_CHUNK_DROP_COUNT_INCREMENTED` and zero reconnect or close events** after the drop; heartbeats kept firing, so the app was alive with transcription dead. Stop then failed with `REQUIRED_STEP_FAILED: audio_queue_not_drained`, `final_status: failed`. This is the third time on this project that reading code produced a confident wrong answer that driving it overturned. |
| 2026-08-12 | Opus 5 | **Root cause: no websocket keepalive.** Both `ws.run_forever()` calls (initial connect and reconnect) were made with no `ping_interval`/`ping_timeout`. A WiFi drop kills TCP without a FIN, so the socket goes *silent* rather than closed; `run_forever()` blocks indefinitely on data that never arrives, `_deepgram_on_close` never fires, and `_schedule_reconnect()` is therefore never called. Every part of the reconnect chain — and the gap marker built for item 44 — was unreachable. Fixed with `DG_WS_PING_INTERVAL_S = 10` and `DG_WS_PING_TIMEOUT_S = 5`, giving ~15s worst-case detection, comfortably inside the 36s outage that was tested. `ping_timeout` must stay below `ping_interval` or websocket-client raises at runtime; a test pins that. |
| 2026-08-12 | Opus 5 | **Second finding from the same run, logged not fixed (§0 rule 3):** `audio_temp/audio_manifest.json` reached **117 MB for 66 chunks** in a 25-minute session — roughly 2 MB per chunk entry in a file that should be a few KB. That is unbounded growth of exactly the kind item 48 exists to bound, and it would not have been visible from code reading. Carried to item 48 as concrete evidence rather than tuned blind. |
| 2026-08-12 | Opus 5 | **English transcript quality — new items 64 and 65, from the same live run plus its reference transcript.** Export `...095935`: 41 lines, 18,589 chars, **16 lines over 400 chars, longest 2,454**, 245 repeated 4-word phrases. This is **not** problem F returning — item 51 fixed whole re-sent sentences being glued together; what remains is (**64**) one- and two-word stutters at chunk seams, which fall below `_overlap_join`'s 2-token and `_tail_resend_splice`'s 3-token minimums by design, and (**65**) no utterance line-breaking at all on the English path, so single lines run to 2,454 characters. Checked against the human-supplied reference: `"schools, schools"`, `"have, have"` and `"Claiming, Claiming"` are **absent** from the source, so they are ours — but the speaker genuinely does stutter elsewhere (`"What What is"`, `"I I I"`), so 64 needs a rule that distinguishes them, not a blanket collapse. |

| 2026-08-12 | Opus 5 | **Items 64 and 65 fixed; item 64's own diagnosis had to be corrected twice before it held.** First attempt compared the export against `evidence_streams/provider_events.jsonl` joined into one string — which makes a repeat spanning a *seam* look like one that arrived inside a single event, the exact thing under test. Second attempt, per-event, said every repeat was already in `raw_text` and therefore not ours. That was wrong too, for a better reason: **on the English path `record_raw_deepgram_final` is called from `_publish_final_transcript_segment`, the COMMIT path**, so `provider_events.jsonl` `raw_text` is our own post-merge output, not the provider's. The comparison was our output against our output. Only driving `_merge_lexical` on the two real spans settled it — all three reference mismatches reproduce character for character. Same trap as Batch 4 item 20b: an evidence file that was never designed to carry what the claim rested on. |
| 2026-08-12 | Opus 5 | **Logged, not fixed (§0 rule 3): `provider_events.jsonl` is post-merge on the English path.** Its rows carry `source: "deepgram_final_ingress"` and a field named `raw_text`, and on the Japanese path (`japanese_final_chunk_stabilizer.ingest`) that is exactly what it is. On the English path the same recorder is called at publish time, so the name is wrong there: run `...095935`'s `raw-000001` is 617 chars at `source_version=60`, which is an accumulation, not a provider payload. `tools/replay_run.py` and `tools/score_run.py` both read this stream. Nothing here depends on it any more, but the next session must not read English `raw_text` as provider truth. Real provider finals need `raw_deepgram_finals.jsonl`, which was empty because retention flags are off. |
| 2026-08-12 | Opus 5 | **Item 64's gate is timing, not a word list, and that is the whole point.** A one-token seam overlap is genuinely ambiguous from text alone — English doubles words across a clause boundary ("He said that" + "that was fine", "the food I had" + "had gone cold"), and collapsing those deletes speech, which this module already ranks as strictly worse than duplication (see `_merge_lexical`'s note on why `_overlap_join` runs before the similarity gate). The audio clock is not ambiguous: a re-send starts before the active utterance ended. Requires at least `_TIMING_OVERLAP_MIN_S` of real overlap so abutting spans cannot read as re-sends, and fails closed with no timing. **Residual risk, stated rather than hidden:** this trusts Deepgram's word timings. If a timing is wrong AND the seam token repeats, one word is lost. The speaker's own stutters ("What What is", "I I I") arrive inside one payload, never at a seam, so nothing here can reach them. |
| 2026-08-12 | Opus 5 | **Suite 495 -> 513 (18 new). Same 7 stable failure names; baseline intact.** One run showed `test_task9_report`'s inactivity-timeout test failing, which matters more than usual here because item 65 changes when commits happen. Checked instead of assumed: 5 runs with the fixes active passed 5/5; 5 runs with both fixes neutralised passed 4/5. It fails *more* without the change — the documented load-flake, not a regression. Both fixes were also verified adversarially: neutralised, 7 of the 18 new tests fail reproducing the original signatures exactly (`'schools, schools'`, and three sentences glued into one line); the other 11 pin unchanged behaviour and stay green either way. |

| 2026-08-12 | Opus 5 | **Live retest run `...20260812-142447` (7 min, drop 14:30:00, restore 14:30:45) — the user's verdict was right on every count, and the causes were three separate faults, not one.** (1) **Item 64 works**: 0 comma-doublings across the run's 5050 committed characters, against ~20 per 18k before; the 17 remaining doublings are all space-separated, i.e. the speaker's own stutters, correctly untouched. (2) **Reconnect still dead**: keepalive detected the close, one attempt ran, it failed because the net was still down, and nothing retried — last commit 14:30:01, 217 audio chunks discarded, `final_status: failed`. (3) **Translation never retried**: `ConnectionException` classified permanent. (4) **Item 65's flush lost 8 of 9 utterances** and is now shipped OFF. |
| 2026-08-12 | Opus 5 | **The reconnect bug is a single-flight guard swallowing its own retry signal**, proven by driving the real `_schedule_reconnect` / `_reconnect_deepgram` / `_deepgram_on_close` against a socket that fails the way a dead network does: 1 attempt, then nothing. After the fix, 2+ attempts with growing backoff over the same window. Worth remembering as a shape, not just an instance: **a single-flight flag makes any retry request that originates *inside* the flight a no-op.** The close handler looked like it scheduled a retry, and it does — into a guard that is guaranteed to be closed at that moment. Nothing in any log said so; the run showed 3 `DEEPGRAM_CLOSE_ERROR` and 1 `DEEPGRAM_AUDIO_GAP_MARKED` and no error at all. |
| 2026-08-12 | Opus 5 | **Item 65 turned OFF rather than shipped, and why that is the right call.** The flush is not broken in itself — it fires exactly where intended and publishes more records than the path it replaced. But on the live run only the single non-flush commit survived to the export. Two probes ruled out the obvious suspects with real data rather than reasoning: replaying the run's 9 real texts through the real `decide_transcript_action` gives `add` 9 times (no silent skip), and feeding the run's real per-commit metadata through the real `_display_transcript_item` stores all 9. So the loss is somewhere between `transcript_events_posted: 10` and the one canonical record, and I could not name it. Shipping a change that loses 89% of a client transcript because the alternative is ugly line breaks would be exactly backwards — §0's own ordering puts silent content loss above every cosmetic problem. `ENGLISH_SENTENCE_FLUSH_ENABLED = False`, tests pin both the mechanism (forced on) and the production default (off). |
| 2026-08-12 | Opus 5 | **Both new fixes verified adversarially; suite 513 -> 526, back to exactly the 7 stable failure names with no task9 flake this run.** Restoring the one-shot reconnect fails the two retry tests and leaves the Stop-respecting and no-spin guards green, which is the intended split. The gap-marker tests pin that marking happens from `_deepgram_on_open` and NOT from the reconnect attempt, and that a first connect marks nothing. The DeepL tests pin that the connection check precedes the substring rules and that quota/auth stay permanent — retrying a quota failure would burn the remaining budget. |

| 2026-08-12 | Opus 5 | **Live run `...20260812-150116` (4 min, drop 15:03:31, restore 15:04:05). Both previous fixes verified working; a third fault found, and it was mine.** Reconnect retried 5 times with the expected 1/2/4/8/16s backoff and reconnected (`DEEPGRAM_CONNECT_END`); the gap marker reported **30.4s** against a real 34s outage, where the old code reported 2.6s for 45s. But at 15:03:50 the app recorded `CRASH_HOOK_TRIGGERED` / `UNHANDLED_EXCEPTION_CAPTURED`: `AttributeError: 'NoneType' object has no attribute 'wait'` in `Thread-6 (_send_ping)`. That thread exists **only because I passed `ping_interval`** — the keepalive that made drops detectable also introduced a bare thread that crashes on teardown. Confirmed against the installed library (websocket-client 1.6.0), not from memory: `_send_ping` reads `self.stop_ping.wait(...)` with no guard, and `stop_ping` is `None` outside `_start_ping_thread`'s assignment. |
| 2026-08-12 | Opus 5 | **Still unexplained after this run, stated rather than papered over:** no transcript between 15:02:11 and the drop at 15:03:31 (80s), and none after the reconnect at 15:04:20 either, although audio was being captured throughout and `AUDIO_CHUNK_DROP_COUNT_INCREMENTED` stops at 15:04:19 — meaning audio resumed flowing to the new socket. The socket was NOT closed during the 80s window: all 5 `DEEPGRAM_CLOSE_ERROR` events are grouped in the outage. The crash at 15:03:50 is the leading candidate for the post-reconnect silence and is now fixed, so the next run separates "the crash killed it" from a second independent fault. Do not assume the former. |

| 2026-08-12 | Opus 5 | **Live run `...20260812-154956` (drop 15:52:30, restore 15:53:02): resilience is CLOSED.** Zero crashes (`CRASH_HOOK_TRIGGERED` n=0 — the ping guard holds), `final_status: completed`, interims ran **15:50:05 → 15:54:46 without a break through the outage**, gap marked 30.7s against a ~32s drop, and 3 commits landed after the reconnect. 3 publishes → 3 canonical → 3 exported, no loss. This also settles the previous run's open question: the post-reconnect silence in `...150116` **was** the `_send_ping` crash, not a second independent fault. Items 44 and 45 are done pending nothing further. |
| 2026-08-12 | Opus 5 | **Item 65 solved as formatting rather than commit timing — the point worth keeping.** Two attempts tried to make English commit more often; the second published 9 utterances and only 1 reached the export. The readable-line problem never required more commits: it required the *readable* text to be grouped, which `TranscriptStore.get_clean_text` already owns as the single builder for both the UI textbox and copy/export. Regrouping cannot lose a word, and `text_is_preserved` asserts that on every case including the real 2342-character line. The user's example is the specification and is reproduced exactly; that test is the one that cannot be bent to fit the implementation. `ENGLISH_SENTENCE_FLUSH_ENABLED` stays False and its unexplained loss stays open as a known risk, not as a blocker. |

| 2026-08-12 | Opus 5 | **Live run `...20260812-161651` (drop 16:19:50, restore 16:20:22): resilience holds a second time.** 0 crashes, `completed`, **7 publishes → 7 canonical → 7 exported**, no loss. Item 64 also holds: **0 comma-doublings**. Item 65's grouping did NOT reach the export, though — `Alpha output.txt` is built from the **frozen canonical ledger**, not from `TranscriptStore`, so wiring `get_clean_text` alone only fixed the UI. Now also applied in `serialize_export_payload`, keeping `lines` 1:1 with `record_ids` (one ENTRY per record, carrying its blocks) so every coverage gate that pairs the two lists is untouched. |
| 2026-08-12 | Opus 5 | **"Repeated words" is a THIRD distinct cause, not item 64 returning.** Run `...161651` has 0 comma-doublings (item 64's seam signature) and 6 space-doublings — `I I`, `he he`, `on on` — which arrive inside a single provider payload and are the speaker genuinely stuttering. `collapse_stutters` removes them from the **readable copy only**; the canonical record and evidence files keep what the provider actually said. Punctuated repeats (`Yes, yes`) are emphasis and survive, as do ordinary English doubles (`that that`, `had had`). |
| 2026-08-12 | Opus 5 | **Tried and reverted in the same session: folding short lines into the previous one.** It looked like the fix for the user's "random short paragraph" — run `...161651` grouped one record into 28, 15, 4, 7, 5, 29 words. But it merges `One. Two. Three.` with `Four. Five. Six.` into a six-sentence line, breaking the max-3 rule the user actually specified, and his own first example line is 13 words, so a short line is correct output when the sentences are short. Reverted; the rule stays sentence-count plus word budget. The comment in the module records why, so it is not re-attempted. |
| 2026-08-12 | Opus 5 | **Known and NOT fixed, needs a design decision: 5 of 7 records end mid-sentence** on run `...161651` (`...never considered`, `...So an`, `...with work`). That is commit timing — `utterance_end` / `inactivity_timeout_fallback` firing mid-sentence — and fixing it means changing when English commits, which is exactly what lost 8 of 9 utterances in item 65's second attempt. Formatting cannot repair it: the sentence is already split across two canonical records. Carried as **item 66**. |

| 2026-08-12 | Opus 5 | **Item 65-flush root cause found; it was never the flush.** The clue was one field. On run `...20260812-142447` the 8 lost commits carried `is_final: False` and the single survivor carried `is_final: None` — and `_display_transcript_item` opens with `if item.get("is_final") is False: return`. `None` passes an identity check; `False` does not. The survivor survived only because its metadata happened to have no `is_final` key. Chain: `_commit_locked` spreads the TRIGGERING event's metadata **last**, so a commit raised while handling a chunk marked `is_final: False` inherits it; `_publish_final_transcript_segment`'s blanket `queue_item.update(metadata)` then overwrites the `is_final: True` it had just set; the display gate drops it before any canonical commit, silently. Evidence that pinned it: 10 publishes, `PIPELINE_COMMIT_TRANSACTION_STARTED: 1`. |
| 2026-08-12 | Opus 5 | **This was never specific to the flush — that is the part worth keeping.** ANY commit raised while handling an `is_final: False` event was discarded the same way; the flush only made it frequent enough to notice. Two earlier probes had already ruled out `decide_transcript_action` (add 9/9) and `_display_transcript_item`'s store path (stores 9/9), and a third against the REAL ledger showed the flush producing 5 publishes → 5 canonical records — all correct, all missing the point, because every one of them constructed the queue item by hand instead of letting the real metadata flow through. The defect lived in the metadata plumbing between them. Fixed at three layers: the commit's own metadata (`_dispatch_commit` sets `is_final: True`), the publish path (re-asserts it after the blanket update), and the drop itself (now logs `COMMITTED_SEGMENT_DROPPED_AS_INTERIM` when an item carrying a commit reason is discarded — the silence is why this took three sessions). |
| 2026-08-12 | Opus 5 | **Adversarially verified.** Restoring the pre-fix `_dispatch_commit` reproduces the live failure exactly: 5 publishes → **1** canonical record, the same 9→1 shape as run `...142447`, with the last one surviving because `on_utterance_end` commits through a different metadata path. With the fix, 5 → 5. `test_task9_report` flaked again this run, so it was checked rather than assumed: 4/6 with the flush ON, 5/6 with it OFF — it fails without the change too, the documented load-flake. |
| 2026-08-12 | Opus 5 | **Run `...20260812-170951` (no network drop, by the user's choice): 0 crashes, 0 `DEEPGRAM_CLOSE_ERROR`, `completed`, 2 publishes → 2 canonical → 2 exported.** Item 65's grouping is now reaching the export: **20 readable lines from 2 canonical records**, 8-63 words each. This run proves nothing new about the network fix (no drop occurred); that evidence is runs `...154956` and `...161651`, which both dropped and both recovered cleanly. |

| 2026-08-12 | Opus 5 | **Item 44 closed — and auditing it against its own title found a requirement that was never built.** The item reads "backoff, buffer, commit in-flight, mark the gap visibly". Backoff, buffer and the marker were all done and live-verified. **"Commit in-flight" did not exist anywhere** — `grep` for it returns nothing, and no run would ever have shown the loss, because it is silent. Driving the real lifecycle: an utterance still open when the socket dies is not merged into post-reconnect text (`_timing_compatible` correctly rejects the provider's restarted clock) and not committed either (`_apply_active_update_locked`'s `force_new` branch replaces `self._active` outright). Measured: pre-drop sentence in, one commit out, and it is the POST-drop one. Every reconnect discarded whatever had been spoken but not yet committed. |
| 2026-08-12 | Opus 5 | **`commit_in_flight()` is called on the first UNEXPECTED close, which is also the right place for ordering.** `_deepgram_on_close` runs before `_deepgram_on_open` emits the gap marker, so the transcript reads pre-drop speech → `[connection lost - approximately Ns...]` → post-reconnect speech — the order item 44's own title asks for. Guarded by the existing `not stop_requested` branch so a normal Stop (which already commits) is untouched, gated on `should_use_utterance_lifecycle` so Japanese keeps the assembler's path, and wrapped so a failure here can never block the reconnect it precedes. The reason is deliberately absent from `_PREMATURE_COMMIT_REASONS` — otherwise the first post-reconnect chunk could extend the record and glue speech from both sides of the hole into one line. |
| 2026-08-12 | Opus 5 | **Lesson worth keeping: an item's own title is a checklist, and "mostly done" hid a whole requirement for four fixes.** Item 44 was reopened three times on symptoms (keepalive, retry loop, ping crash) and each time the fix was real, so the item kept looking closer to done. Re-reading the title and grepping for each of its four nouns is what surfaced the missing one. Suite 563 → 576, exactly the 7 stable failure names, no task9 flake this run. |

| 2026-08-12 | Opus 5 | **Item 66's real defect was duplication, not the mid-sentence split.** Reading the records next to each other is what showed it: `[5]` ends `"...he writes openly, I never considered"` and `[6]` begins `"he writes openly, I never considered him an impostor..."`. The committed tail is stored **twice**, in two canonical records. Mechanism: `utterance_end` and `speech_final` mean *speech* paused, not that a *sentence* finished, so a speaker pausing mid-clause gets committed there; the provider then re-sends the span; and `_overlap_join` / `_tail_resend_splice` only ever run WITHIN one active utterance, so once the previous one is committed nothing dedupes across the boundary. |
| 2026-08-12 | Opus 5 | **The obvious fix was implemented, measured, and REJECTED — it truncates the export.** Treating a mid-sentence commit as premature (so `_extend_committed_locked` absorbs the continuation) is the natural reading, and the gate change worked: `_is_premature_continuation_locked` returned True and `_merge_lexical` produced the correct merged text. But driving it through the **real** ledger showed the canonical write being skipped as `already_committed`, so the lifecycle and the `TranscriptStore` held `"...I never considered him an impostor at all"` while the LEDGER kept `"...I never considered"` — and the export is built from the ledger. That is content truncation, strictly worse than the duplication being fixed. Reverted. **Worth remembering: in this codebase a lifecycle-level fix is not done until it has been driven through the real canonical ledger, because the store and the ledger can disagree silently.** |
| 2026-08-12 | Opus 5 | **Shipped instead: trim the NEW utterance's head, never revise the committed record.** `_strip_committed_tail_prefix` removes only a leading run that is provably a suffix of the text already committed, so nothing is lost — those words are in the previous record verbatim. `min_run=3` matches `_tail_resend_splice`, because this can discard text and a two-word echo across a genuine boundary (`"...thank you."` / `"You are welcome"`) must not trigger it. Gated on same channel, `speakers_confirmed_same`, and NOT after `provider_disconnected` — words either side of a network hole are unrelated, so an overlap there is coincidence. Measured on the real pair: 2 records, each word once. Suite 576 → 591, same 7 names. |

| 2026-08-12 | Opus 5 | **Audited 25 / 27 / 27b / 50 for risk before touching any of them; only 50 was safe to change.** **27 is closed by measurement, not by code**: the item asked to decide from evidence, and `OBSERVE_IDENTITY_FAILED_OPEN` has fired **0 times across all 43 recorded runs**, while fail-closed there means rejecting a commit — content loss. **27b stays deferred**: its premise is sound but re-deriving 0.72 needs a labelled corpus that does not exist, and blind threshold tuning is the exact mistake item 34 was superseded for. **25 closed won't-fix**: separate `jpm-utt-` namespace, manual mode only, and the code is a deliberate earlier fix. |
| 2026-08-12 | Opus 5 | **Item 50 shipped — and its first version broke translation, caught by an existing acceptance gate.** I guarded the obvious risk (an older deepl SDK rejecting the `context` keyword) and missed the real one: `translate_text`'s signature is a **contract other code implements**. Five implementations in this repo — four test doubles plus `tools/validate_utterance_revision_repair.py` — take only `(text, source_lang, target_lang)`, so passing `context` raised TypeError inside the worker's translate loop and DROPPED the translation. `test_task3c_acceptance_gate`'s "final Japanese line's translation must not be dropped on Stop" failed deterministically, 3/3. Fixed in the production code (detect on the client object, offer the keyword only to clients that declare it) rather than by editing the stubs — editing them would have hidden a real compatibility break from any third-party or older client. |
| 2026-08-12 | Opus 5 | **Worth keeping: feature-detecting the library was not enough; the seam that broke was our own interface.** A signature that other modules implement is an API, and adding a parameter to it is a breaking change even when every in-repo caller is updated. Suite 591 → 608, back to exactly the 7 stable failure names. |

| 2026-08-14 | Opus 5 | **Live run `...20260814-101813` (5 min, drop ~10:20:30, restore ~10:21:05) — 44, 64, 65 confirmed working; 66 found one survivor and was widened.** `final_status: completed`, **0 crashes, 0 `THREAD_EXCEPTION_CAPTURED`, 0 `DEEPGRAM_CLOSE_ERROR`**, gap marked **31.0s**, interims ran 10:18:23→10:23:24 unbroken across the outage, 5 commits after reconnect. **Item 64:** the only comma-doubling is `"many, many things there"` — ordinary English, not the seam signature; the 5 space-doublings are speaker stutters, correctly untouched. **Item 65:** 6 canonical records rendered as **31 readable lines**, only one over 60 words. **Item 50** is live (no errors). |
| 2026-08-14 | Opus 5 | **Item 66's speaker gate was too strict for a provider whose diarization disagrees with itself.** One duplication survived: `[4]` ends `"...and the number 1 thing"` (speaker 2), `[5]` begins `"And the number 1 thing that I hear..."` (speaker 1). Same words, same audio — their spans overlap by ~1.9s — but two different labels, so `speakers_confirmed_same` refused and the trim never ran. Overlapping audio is now an alternative qualifier to the speaker match. It is an OR, not a replacement: a real speaker change cannot produce overlapping spans, and with timing absent `_audio_spans_overlap` fails closed, so items 22/23/24's guard remains the only rule exactly where it has to be. |
| 2026-08-14 | Opus 5 | **A stale test premise, caught by the change rather than by review.** `test_a_different_speaker_is_never_trimmed` used candidate timings that OVERLAPPED the previous span, so once overlapping audio became a qualifier it began asserting the opposite of its own name. Renamed and re-timed to disjoint spans so it tests the guard it describes. Worth remembering: a test whose fixture accidentally satisfies a condition it never mentions will pass for the wrong reason until that condition starts mattering. Suite 608 → 610, same 7 stable failure names. |
| 2026-08-14 | Opus 5 | **Item 44's one remaining gap, diagnosed and deliberately NOT fixed: the gap marker never reaches the exported transcript.** Run `...101813` emitted it correctly — `raw-000003: "[connection lost - approximately 31s of audio not captured]"` — and it reaches the live UI store, but not the canonical ledger, so it is absent from `Alpha output.txt`. Cause: the marker is synthetic and carries no `source_raw_event_ids`, while `RAW_EVENT_LINEAGE_REQUIRED = True` means every canonical record must trace to a real provider event. So "mark the gap **visibly**" is delivered live but not in the export. Fixing it means either weakening that lineage rule or injecting the marker at export-render time — both are export-authority changes, and neither is worth doing days before delivery on an item whose other three requirements are verified. Carried as **item 67**. |

| 2026-08-14 | Opus 5 | **Items 68 and 69 done — 68's real work was proving the placeholder is not an ordering slot.** It looked like a one-line deletion. It is not: the row sets a Tk mark `tr_load_{segment_id}`, and the removal path deletes `mark -> mark lineend + 1 chars`. Skipping only the TEXT would leave that mark at `"end"` with nothing under it — Tk marks have right gravity, so it would sit **before** the next appended line and that delete would remove a real translation. The mark is therefore gated together with the text. Both removal sites already wrap `box.compare(...)` in `except Exception`, so an absent mark is a no-op; a dangling one would not have been. Ordering was checked separately and is safe: the finished translation is appended at `tk.END` regardless of where the row sat, and sequencing comes from the worker's `translation_sequence` buffer. |
| 2026-08-14 | Opus 5 | **Item 69 was the cheap one and it is why it went first.** The preview is deleted (`interim_anchor` → `end`) and rewritten on every tick, so emitting several lines instead of one is structurally safe — they are all removed together, and no committed text is involved. Only the last line carries the ⏳; repeating it on each line reads as several pending utterances rather than one in progress. Falls back to a single line on any failure, deliberately: this executes on the UI thread, where raising is worse than not grouping. Suite 610 → 623, same 7 stable failure names. |

| 2026-08-14 | Opus 5 | **Quick test after items 68/69 (2 min en→jp, 2 min jp→en): both runs `completed`, 0 crashes, 0 `COMMITTED_SEGMENT_DROPPED_AS_INTERIM`, EN 6 pub → 6 canonical → 6 export, JA 51 → 12 → 12. No new errors.** The repetition the user saw on the EN side was one pair, and the log showed something better than a miss: **`RESENT_TAIL_TRIMMED` fired for `"The 3rd 1"` and the exported record still began with it.** |
| 2026-08-14 | Opus 5 | **Item 66's trim was correct but not DURABLE — that is the finding worth keeping.** Trimming only when the utterance is created assumes the provider sends forward-only text. Deepgram re-sends its window **cumulatively**, so the next chunk arrived carrying the head that had just been removed, `_merge_lexical` took it wholesale (`prev_n in curr_n` → return curr), and the duplicate was back; that utterance went on to `source_version=8`. The trim is now re-applied after every merge. It is idempotent by construction — `_strip_committed_tail_prefix` matches only at the HEAD, so once removed it returns None and nothing changes, while a cumulative re-send that reintroduces it is trimmed again, and a later mid-text recurrence of the same words is never touched. |
| 2026-08-14 | Opus 5 | **Japanese repetition checked and NOT ours.** The JA run's repeats (`こんなに` ×2, `春だった` ×2) sit INSIDE single records, and Japanese never enters `UtteranceLifecycleOwner` at all (`should_use_utterance_lifecycle` is English/generic only) — it uses the assembler's own boundary path, so item 66's trim cannot apply there by design. No cross-record duplication was found on the JA side. The visible oddities are speech disfluency plus STT errors (感想 for 乾燥), not pipeline duplication. Suite 623 → 627, same 7 stable names. |

| 2026-08-14 | Opus 5 | **1-hour test (99 min, run `...20260814-114309`) — the cleanest run of this sprint.** `completed`, **0 `CRASH_HOOK_TRIGGERED`, 0 `THREAD_EXCEPTION_CAPTURED`, 0 `COMMITTED_SEGMENT_DROPPED_AS_INTERIM`**, 161 publishes → 160 canonical → 160 exported, and translation **161/161 successful, 0 failed, 0 retries** (p50 662 ms, p95 1831 ms). Every recent item held at scale: **item 66 fired 37 times and left 0 cross-record duplications**; item 65 rendered 160 records as 430 readable lines (median 24 words); item 64 produced 12 comma-doublings of which **8 of 9 distinct forms are verified present in the human reference** (`really really`, `great great`, `ambivalent ambivalent`) — only `interesting, interesting` is not, once in 60,294 characters. |
| 2026-08-14 | Opus 5 | **Item 48 measured properly for the first time, and the stability half is clean.** Queues (audio, transcript UI, event bus, async log) read **zero at every one of 197 health snapshots**. Memory rose to ~258 MB during the first 25 minutes then grew **13.5 MB over the remaining 74** — warm-up followed by a plateau, not a leak. `transcript_store_count` tracked content (50→159) rather than time. The one genuinely unbounded thing was disk. |
| 2026-08-14 | Opus 5 | **`audio_manifest.json` root cause: 193,675 per-packet records, 98.9% of them silence.** 117 MB total, `packets` alone 95 MB — 191,579 `SOURCE_SILENCE` against 2,096 `ACTIVE`, and **every single entry had `retained_frame_count == source_frame_count`**, so each proved only that nothing was dropped. It is also the most plausible cause of the run's **498 MB peak RSS against a 272 MB steady state**: serialising a 95 MB list. Consecutive silence per stream is now one run entry carrying counts, frame/byte totals, time span and an `all_frames_retained` flag that any single dropped frame flips false. The app is called "Preserve Real Silence" — the evidence needed is that silence was RETAINED, not that it was retained 191,579 separate times. ACTIVE packets keep full per-packet fidelity. Per-packet `run_id` dropped (51 chars × 193,675 ≈ 9.8 MB on its own). |
| 2026-08-14 | Opus 5 | **The pre-existing cap was a silent one.** `if len(packets) > 200000: packets = packets[-100000:]` discarded the first half of a session with nothing recorded to say so — exactly what §0 forbids. The backstop now counts what it drops into `packet_entries_dropped`, and sits at 40,000, far above what a collapsed session produces. Three of the new tests drive the REAL `_ingest_audio_chunk_impl` rather than a re-implementation, because a stub copied from the code it tests is the shape that made item 65's diagnosis wrong three times. `test_task9_report` flaked again and was checked, not assumed: **3 of 4 failures both with and without this change** — identical, so it is the documented load-flake. Suite 627 → 639. |

| 2026-08-14 | Opus 5 | **The 7-minute run `...20260814-142929` proved my own item 48 fix was a special case, not the mechanism — and it is the run that shows why "fixed" needs a live check.** The fix WAS in the build (committed 13:50, run started 14:29) and WAS working: 272 collapsed runs, no per-packet `run_id`. But that run was **36,251 ACTIVE packets against 272 silence runs** — the exact inverse of the 99-minute run's 98.9% silence — so the manifest still reached **19.32 MB in 7 minutes, ~2.8 MB/min against 1.2 MB/min BEFORE the fix**. Collapsing only silence helps only when the room is quiet. |
| 2026-08-14 | Opus 5 | **Generalised: every routine classification collapses per stream, not just silence.** Measured on that run's own data, per stream rather than across the interleaved list — which matters, because three streams rotate so a naive contiguity check sees 36,251 packets as 35,906 "runs" and concludes nothing is collapsible. Per stream it is mic 51x, mixed 259x, system 1104x: **62,937 packets → 507 entries, ~0.15 MB instead of 15.43 MB**. Driving the real ingest with that shape gives 21,000 packets → 22 entries at 0.010 MB with every packet still accounted for in `packet_count`. A classification change closes the run so the silence/speech boundary stays visible, and UNKNOWN / CAPTURE_GAP stay per-occurrence because each one IS the forensic signal. |
| 2026-08-14 | Opus 5 | **Two of my own tests had encoded the special case and had to be superseded, not patched around:** `test_real_active_packets_are_not_collapsed` asserted the very behaviour that was wrong. Renamed and inverted. Worth keeping as a pattern: when a fix is a special case, its tests pin the special case, so generalising the fix must generalise the tests — a test that still passes after the generalisation may simply be asserting the narrower rule. Suite 639 → 644, same 7 stable names. |

| 2026-08-14 | Opus 5 | **Pre-run xhigh review of the whole session's diff — three defects found in my OWN recent work, all fixed.** (1) **`get_clean_text` was O(session) on the UI thread.** It re-derived the readable lines for EVERY segment on EVERY render, while only the newest can have changed: measured 14.9 ms at 20 segments, 29.0 at 160, **62.0 at 320** — against `UI_QUEUE_TIME_BUDGET_MS = 10`. That is the mechanism behind the 99-minute run's **162 `UI_EVENT_DRAIN_TIME_BUDGET_EXCEEDED`** events, and a 3-hour meeting would spend ~100 ms per render regrouping unchanged text. Memoised per segment, keyed on the TEXT (not identity) because `update_last_segment_if_active` rewrites in place. **62 ms → 3.3 ms at 320 segments.** |
| 2026-08-14 | Opus 5 | **(2) `re.IGNORECASE` on `[A-Z]` made it match any single letter.** A sentence ending in a one-letter word ("I take vitamin a. Then...") was read as an initial, so the boundary after it was never taken and two sentences merged. Split into a case-SENSITIVE initial pattern and a case-insensitive abbreviation pattern. **(3) `collapse_stutters` was collapsing repeated NUMBERS** — `"8 8 percent"` became `"8 percent"`. `1 1` appears in this project's own real transcripts as list items or as a decimal the provider split; collapsing a digit changes a number rather than removing a stumble, so numeric tokens are now excluded. |
| 2026-08-14 | Opus 5 | **The existing suite caught me over-reaching on the regex fix.** My first version added a trailing `\.?` to the initial pattern, which the original never had — so `"A."` itself became an initial and `"A. B. C."` stopped splitting at all. `test_a_long_line_is_never_produced_by_merging` failed with 9 sentences on one line. The candidate text ends just BEFORE the boundary period, so an initial looks like `...J` and not `...J.`; only the case-sensitivity was ever wrong. Worth keeping: when fixing a regex, change one property at a time — the second change rode in unnoticed and would have silently disabled sentence splitting for initial-heavy text. Suite 644 → 655, same 7 stable names. |

| 2026-08-14 | Opus 5 | **Item 48 CLOSED. Run `...20260814-155844` (8 min) confirms the manifest fix live: 117 MB → **0.263 MB**, 354 entries covering 71,181 packets, 100% collapsed, no truncation marker.** Per minute that is 1.18 MB → 0.033 MB, so a 3-hour session costs ~6 MB instead of ~210 MB. Content also clean: 22 pub → 22 canonical → 22 exported, **0 cross-record duplications**, **0 `… ⏳` rows in the readable output**, 22/22 translations, 0 crashes. |
| 2026-08-14 | Opus 5 | **Local test against the real recorded audio, not a synthetic fixture.** `tools/replay_run.py` cannot replay an English run — it correctly reports "no genuine provider ingress" because English records commits, not ingress (the §9 trap) — so the test that actually validates the item 48 change is reconciling the manifest against the WAVs. **24/24 chunk sha256 verified**, and every collapsed run's `retained_frame_count` matches the frames on disk exactly: 7,592,640 per stream across all three. `all_frames_retained` true on every run. The aggregate proves what the 193,675 individual records proved, at 1/200th the size. |
| 2026-08-14 | Opus 5 | **A false finding caught before it was reported, and a real one found behind it.** I grepped for a Tk rendered-line cap under the wrong names and was about to report it missing — it exists (`MAX_RENDERED_UI_SEGMENTS = 500`, debounced, store-authoritative). Reading the render path to confirm that instead surfaced a genuine gap: **`_render_transcript_from_store_now` rendered `seg.text` RAW**, so item 65's grouping reached copy/export and `Alpha output.txt` but never the pane watched during the meeting — while item 69 had already grouped the interim preview above it, so text visibly reflowed the moment it committed. Now reuses the store's memoised `_readable_parts`: 30 paragraphs → 60 lines, median 24 words, **0.3 ms to render at the 500-segment cap**. Suite 655 → 659. |

| 2026-08-14 | Opus 5 | **Item 67 done without weakening the lineage rule, and retention restored.** The marker reached the live UI store but never `Alpha output.txt`, which is built from the frozen ledger — and it can never be a ledger record, because it is synthetic with no `source_raw_event_ids` and `RAW_EVENT_LINEAGE_REQUIRED` means every canonical record traces to a real provider event. Rather than carving an exemption into that invariant, gaps are recorded beside the ledger and rendered at export. The placement detail is the load-bearing one: the marker is **prepended into the entry of the record that follows the outage**, never emitted as its own line, so `lines` stays 1:1 with `record_ids` — the same constraint item 65's grouping had to respect, and the reason a naive insert would have broken every coverage gate that pairs those lists by index. |
| 2026-08-14 | Opus 5 | **`TEMP_AUDIO_RETENTION_ENABLED` / `TEMP_AUDIO_AUTO_DELETE_ENABLED` restored to True** at the user's request. They were set False on 2026-08-11 to protect item 40's reference recording, which is long captured; leaving them off ships an app that never cleans up after itself — the 99-minute run alone left **662 MB** of WAV in `audio_temp`. Both must agree: `cleanup_old_audio_temp()` only skips when BOTH are off, so setting one without the other silently keeps retention disabled. Suite 659 → 670. |

| 2026-08-14 | Opus 5 | **11b-LT PASSED.** Run `...20260814-165109`: spoke, paused ~10 s, Stop. The final sentence survived into the export, `incomplete_stop_tail.txt` is empty and nothing was suppressed. The only imperfections are STT ("LT" → "l t.", "11b" → "11 b"), not pipeline loss. |
| 2026-08-14 | Opus 5 | **Reviewing item 67 found two defects in it, one cause, before any live run hit them.** Emitting each outage as its own line prepended them in turn, so **multiple gaps before one record came out in REVERSE chronological order** (10 s + 40 s rendered 40 s above 10 s). And two outages of the same rounded length with no speech between them render **identical adjacent lines, which `sweep_residual_duplicates` collapses to one** — verified by driving the real sweep: 2 markers in, 1 out. That would have silently understated lost audio in exactly the flaky-connection case the marker exists for. |
| 2026-08-15 | Sonnet 5 | **Audit: §1/§3/§4's baseline was three revisions stale, and item 50's status was wrong.** §3 claimed 489 tests / 2 skipped; the real suite is **674 / 5F / 2E**, same 7 names. Skips are 2 or 3: the third (`test_english_line_grouping…test_words_survive_the_real_2342_character_line`) `skipTest`s itself when run `...20260812-154956` is absent, and retention has since deleted it (43 runs → 11). **Not a regression — but the test's guarantee vanishes silently with its evidence**, so its pass must not be read as ongoing coverage. Item 50 was still marked OPTIONAL although `3514999` had shipped it; corrected in §8 with what was actually built. |
| 2026-08-16 | Opus 5 | **Item 72's severity was overstated on 2026-08-15 — correcting it.** The earlier note implied a client silently receives a blank file. `canonical_finalize.py:214` already guards `active_record_count == 0` and returns `empty_stable_reconstruction` before `serialize_export_payload` is reached, and `run_artifacts.py` logs `EMERGENCY_UNVERIFIED_TRANSCRIPT_WRITTEN` with `reason="no_committed_final_source"`. So the run **is** flagged. What was missing is narrower and still worth fixing: neither the file nor the log says *the network dropped for 45s*, which is the one fact that explains the emptiness. Fixed in `df1ecc1`. |
| 2026-08-16 | Opus 5 | **Item 70's stated root cause is only half right, and the correction matters for anyone reading it later.** The mechanism is real — `_merge_lexical`'s `curr_n.startswith(prev_n) -> return curr` makes `merged == curr`, which the flush's pure-append equality rejected. But the ledger's "the flush rarely triggers" is **wrong**: on run `...20260814-114309` the flush was the *dominant* commit reason, **115 of 160**, and across six English runs **160 of 221** commits (72.4%). The accurate statement is that the flush fires on most LINES while missing most BOUNDARIES: 921 sentences committed, 115 flushes — roughly **1 boundary in 8** — at a mean `source_version` of **29.8** (about 30 merges per committed record), leaving 57 of 160 records over 400 characters, longest 1556. Rate is **1.6–4.2 commits/min**, not the "~1/min" the item claims. |
| 2026-08-16 | Opus 5 | **Item 67's live-UI half is DEAD, and this is the FOURTH "shipped but unreachable" in this project.** Found while designing item 73's surfacing. Run `...20260814-101813` logs `DEEPGRAM_AUDIO_GAP_MARKED` at 10:21:15.842, then `IDENTITY_REJECTION reason="missing_identity_key"` at 10:21:15.938 — and the string `"connection lost"` appears **nowhere** in that run's `transcripts/`. `_publish_final_transcript_segment` never mints a `canonical_utterance_id`, so `duplicate_protection` rejects the synthetic marker and returns before the store write. The test could not catch it because `_GapHost` stubs the publisher. **Item 67's ledger/export half has also never produced output in a real run** — only two runs ever logged the gap and neither has the marker in its transcripts; the one that did predates the fix by hours. So item 67 is unit-tested and unobserved. Item 73 was deliberately routed away from that path because of this. The stale docstrings claiming the marker "reached the live UI store" (`tests/test_item67_gap_marker_in_export.py`, and the comment at `deepgram_client.py:2372-2377`) need a visible retraction. |
| 2026-08-16 | Opus 5 | **Item 73 detection shipped (`55fc90c`); two of my own bugs caught before it landed.** (a) The ctypes vtable helper derived argtypes from `byref()` values — that yields `CArgObject`, which silently builds a wrong prototype, so **every endpoint read returned `""`** and the watcher would have been permanently blind while looking healthy. Caught by driving the real COM calls by hand and comparing HRESULTs. (b) The first draft **re-baselined onto each new default**, so a user switching BACK to the capture device — a recovery — was reported as a second fault, and the sticky warning could never come down. The baseline is now the bound device and never moves, with a separate `AUDIO_OUTPUT_DEVICE_RESTORED` path. Also caught: one of my new tests was **vacuous** — patching `ctypes.oledll` with a `MagicMock` does not raise, so the `except` branch never ran and the assertion passed through the NULL-pointer path instead; replaced with an object that raises on attribute access and confirmed the branch is now exercised. |
| 2026-08-18 | Opus 5 | **Correction to the 2026-08-11 entry below: `test_task9_report`'s real-thread stop test is not "flaky under full-suite load".** That entry rested on the module passing 9/9 across 3 solo runs. Run 8 times instead, it fails **3 of 8 in isolation** — 3 runs was too small a sample to see a ~30% flake, and load is not the trigger. The entry's operative conclusion still holds (not a regression, not caused by that session); only the mechanism was wrong. Now filed as item 78 rather than left as an unowned "human call". |
| 2026-08-18 | Opus 5 | **Three of phase 3b's own design readings were WRONG, found by an adversarial audit of it, and corrected in 3c rather than quietly rewritten.** (1) 3b read `.atf-listening-group, .atf-action-group { flex: 1 1 100% }` as "each group takes a full row" and stacked them. `flex-wrap` appears on exactly three selectors in the design file and `.atf-footer` is not one of them, so the footer is `nowrap` and two `flex-basis: 100%` children **cannot** wrap — they shrink against each other side by side. The wrapping belongs to `.atf-action-group`, which does carry `flex-wrap: wrap`. (2) `_footer_button_width` divided by the scaling factor on the claim that `CTkFont.measure` returns device pixels; measured, it returns **90** for "Start Listening" both before and after attachment against **138** device px on the rendered label — it is already in unscaled units. The division returned 92 where 93 was needed and escaped notice only because a CTkButton grows past its configured width. (3) The stacked reading grid is **57.5/42.5** (`grid-template-rows: minmax(0, 1.15fr) minmax(0, .85fr)`), not the 70/30 3b carried over from the columns. **Lesson: reading a CSS rule is not reading the cascade** — `flex: 1 1 100%` means nothing until you know whether the container wraps. |
| 2026-08-18 | Opus 5 | **Tk's `minsize` is not CSS `minmax`, and using it as one silently destroyed the ratio it was meant to protect.** The design's column track is `minmax(220px, 30fr)`. Tk reserves `minsize` **before** applying weights and then adds the weighted share on top, so an unconditional `minsize=220` inflates the column instead of clamping it — measured, it turned 70/30 into **57/43 at 1200 design px**, where a 220 floor should not have been in play at all. The floor is now installed only when it actually binds, and replaces the weight rather than adding to it: measured **70.0/30.0 from 740 to 1400**, floor engaging below. `RIGHT_COLUMN_MIN_WIDTH` had sat in `theme.py` with zero consumers since the first UI pass — a fifth instance of this repo's "shipped but unreachable" shape. |
| 2026-08-18 | Opus 5 | **Workflow audit of phase 3b: 17 agents, 9 died on session limits** (4 of the 5 investigators and 5 verifiers). Only the design-spec lens survived intact, which is what caught the three misreadings above — the ratio, footer-code, title and sweep lenses never ran. **Recorded because the surviving output looked like a complete audit and was not:** its "3 confirmed / 8 refuted" counts are an artifact of dead verifiers being scored as refutations, and its claim that the ratio needed no fix came from measuring the working tree *after* the fix was already applied. Everything acted on here was re-measured directly before use. |
| 2026-08-18 | Opus 5 | **The responsive breakpoints were being compared against DEVICE pixels while the design's are CSS px — the root cause behind several of the user's complaints at once.** `winfo_width()` reports device pixels and CustomTkinter runs the window at `ScalingTracker.get_widget_scaling` (1.5 here), so a window created at 900 reports **1350**. Every threshold was therefore deciding the layout from how many dots the window covers rather than how much content fits, and the same physical window laid out differently on a 100% and a 150% machine — which is exactly the situation on the client's unknown laptop. New `_design_width()` divides the factor out; the reading grid, the footer and the type scale all ask it. It also exposed a second-order bug: the reading grid stacked on `mode != "wide"`, i.e. below **1050**, where the design keeps two columns down to **700**. |
| 2026-08-18 | Opus 5 | **The 70/30 pane ratio was unreachable, not merely mis-set — and the synthetic test that said otherwise was measuring empty frames.** Tk's grid gives a column at least its children's requested width and shares only the remainder by `weight`. Two floors sat under both columns: a `CTkFrame` defaults to `width=200` (300 px once scaled), and `_create_styled_text` builds its `tk.Text` with no `width=`, so Tk's default of **80 characters** requests **644 px**. Measured with the real card structure the panes came out **49/51 in a 900 px window** and **31/69 at 700** — the reference transcript more than twice the primary pane. Fixed by requesting 1 px on the three column frames and setting `grid_propagate(False)`: measured **70.0/30.0 at 700, 900, 1200 and 1400**. The `tk.Text` default then stops mattering, so it was left alone. **Lesson: a geometry test over empty frames proves nothing about a ratio** — the whole mechanism is children's requested widths. |
| 2026-08-18 | Opus 5 | **The footer hid the one control a meeting cannot proceed without.** Driven against the pre-change code: with `_compact_mode = True` (any width below 800) `_apply_footer_layout` called `left_controls_frame.grid_remove()` and gridded `clear_btn` alone — measured, Start, Copy and Export were all unmapped and **Clear was stretched across the entire footer**. That is precisely what the user reported. Nothing in the design hides the start/stop button at any width. Separately, the fixed widths clipped labels: the rendered button font is 20 px, so "Start Listening" needs 138 px of glyphs plus padding while `FOOTER_BTN_WIDTH_COMPACT` (88) buys 132 — and the same arithmetic clipped "Copy Translation" (184) and even "Meeting Summary" (198 against `FOOTER_BTN_WIDTH_SECONDARY`'s 188) at full width. Widths are now measured from the label, which is what the design does (it sizes by padding, not by a fixed width). A `CTkButton` does not clip itself — it requests text + padding — so the visible clipping came from four buttons sharing one row: once the requests exceed the row Tk shrinks all of them below request. The design's own answer is `flex: 1 1 100%` per group below 700 px, i.e. two rows. |
| 2026-08-18 | Opus 5 | **Three footer breakpoints (680 / 700 / 800) disagreed with each other and with the reading grid.** `LAYOUT_FOOTER_WRAP_BREAKPOINT` (680) sat *below* `LAYOUT_MEDIUM_BREAKPOINT` (700) and `LAYOUT_HAMBURGER_BREAKPOINT` (800) keyed a completely different branch, so between 680 and 800 the footer took a shape neither the design nor the reading grid used. Reduced to the design's own two: **700** (each group takes its own row) and **430** (the action buttons share that row). Measured across 400-1400 px after the change: no clipped label, no overlap, nothing past the footer edge, all four buttons mapped at every width. |
| 2026-08-18 | Opus 5 | **Item 71 phase 3d, filed against a live user report with a screenshot: the footer was broken on EVERY launch, not just narrow ones, and the fix from 3b/3c only ever wrapped the cutover width around, never removed the wrap.** `create_footer()` runs during `__init__`, before `self.geometry("900x650")` is realized by the window manager; measured, at that instant `winfo_width()` returns Tk's own placeholder (200 device px), not `<=1`, not 900 — 200/1.5 is a 133 design-px window, which is what the footer built itself for and stayed at until something else re-ran the responsive layout (why resizing "fixed" it). New one-shot `<Map>` handler `_on_first_map` corrects it the instant the window is really on screen. Separately, the wrap-avoidance threshold moved from a fixed number to the ACTUAL computed line count -- measured, "Copy Translation" alone is wider than half the row from 400 up to ~550 design px, so any single fixed cutoff either left the wrap active past it or swallowed widths where one line was possible; a follow-up screenshot at the ORIGINAL 400px cutoff proved this exactly. Also fixed in the same pass: Start/Stop removed from the hamburger menu (was duplicated with the footer's own), and the two language dropdowns now default to different languages instead of both "Japanese". Suite 753 → 762. |
| 2026-08-18 | Opus 5 | **Item 71 phase 3e, filed against a THIRD round of user reports (two header screenshots plus a footer follow-up): the header's entire mode-switching chain compared design-px thresholds against device px, and it was not caught in 3b/3c because it is driven by `_apply_responsive_layout` rather than calling `_design_width()` itself.** Measured: a window at 900 design px reports 1350 device px on this display, so `_get_layout_mode(1350)` returned **"wide"** (1350 >= `LAYOUT_WIDE_BREAKPOINT` 1050) for a window that should have been "medium" — the header applied NONE of `_pack_header_controls`'s narrowing at a width that could not fit it, which is what pushed the swap toggle and Meeting Summary off-screen. Fixed the same way as the footer/reading grid: compute `design_width` once via `_design_width()`, thread it through the whole chain. |
| 2026-08-18 | Opus 5 | **That fix alone did not show up live — driving the real app surfaced a SECOND, deeper bug: the header's first responsive pass was skipped forever on a normal launch, not merely delayed.** `_on_first_map` (added in 3d) called only `_apply_footer_layout`/`_refresh_reading_typography` directly, never the header. `_mark_real_alpha_interactive_ready` separately snapshots `_last_layout_width`/`_last_layout_mode_applied` from the window's real size WITHOUT ever calling `_apply_header_layout`, and `_apply_responsive_layout`'s own `UI_PERFORMANCE_MODE` skip-check compares against exactly those two attributes — so once that snapshot lands, the header's own correction is never reachable, only the footer's (which 3d fixed directly, bypassing the skip-check entirely). `_on_first_map` now runs the FULL `_apply_responsive_layout()` and invalidates the snapshot immediately before calling it, so ordering between the two mechanisms cannot suppress the one pass that applies anything. Verified against the real running app at three widths (default launch, maximized, and the exact narrow width from the user's screenshots) — not only the test harness, after this exact class of bug already escaped harness-only verification once in this item. |
| 2026-08-18 | Opus 5 | **Item 71 phase 3e also carries the footer's remaining ask from the same report: Start/Stop read taller/wider than the other three buttons, and alignment changed shape between 400–700px.** Both were explicit, permanent overrides of the design's own CSS rather than bugs in implementing it: one shared height for all four footer buttons (the design's 4px Start/Stop difference read as a mismatch, not a hierarchy), and Start/Stop always left-aligned / the action group always right-aligned at natural width, never stretched, at every width up to the hamburger cutover. The wrap-avoidance threshold (phase 3d) was corrected in the same pass to subtract Start/Stop's real width from the row instead of assuming a 50/50 split — more accurate, and it moved the hamburger cutover from ~560 down to ~410 design px. Suite 762 → 777. |
| 2026-08-18 | Opus 5 | **A CustomTkinter font size is PIXELS, not points — and the two reading panes get no display scaling at all.** Measured on 5.2.2: `CTkFont(family="Segoe UI", size=14)` and `tkinter.font.Font(size=-14)` have identical metrics, so the design document's `font-size: 18px` is `size=18` and a px-to-pt conversion would have shipped the client text a quarter smaller than specified. Separately, CTk scales its own widgets by `ScalingTracker.get_widget_scaling` but the two reading panes are raw `tk.Text` and receive none of it: **on this 150% display one `CTkFont(size=16)` object renders at 18 pt inside a `CTkLabel` and 12 pt inside the `tk.Text`**, so the app's primary content was a third smaller than the chrome around it — a pre-existing defect that would follow the build onto any scaled laptop. `_design_px` now applies the same factor. |
| 2026-08-18 | Opus 5 | **`spacing1` is resolved from the tags on the FIRST CHARACTER of a display line, not from the tag carrying the text.** Every rendered entry begins with the `speaker_label` tag, so the paragraph rhythm configured on the `body` tag applied to nothing on exactly the lines it was written for — and would have failed silently, since the text still renders. Moved to the widget, where the `interim` tag can still override it for the live preview row. Related: four sites build `speaker_label` lazily with a hardcoded `("Segoe UI", 12, "bold")` — raw Tk, so 12 *points* and unscaled — which happened to match the old body size exactly; it is now configured up front, which wins because all four are guarded by `if tag not in box.tag_names()`. |
| 2026-08-18 | Opus 5 | **This environment cannot measure a UI toggle, only a first paint.** After `grid_remove()` a sibling's width does not recompute and `winfo_ismapped()` stops tracking — verified directly: two 50/50 panes in a 600 px window measure 300/300, and removing one leaves the other at 300 instead of 600. Earlier readings that looked like "the transcript never maps" were this artifact, not a defect. Every item 71 geometry test therefore builds its own root and measures one configuration, which is accurate but leaves hide/show **transitions** and stacked reflow uncovered. `UI_REDESIGN_PROMPT.md` already states a green suite is not evidence here; a live visual check is the only cover. |
| 2026-08-19 | Sonnet 5 | **Item 78 root-caused by driving the real code, not by reading it -- the flaky test was never exercising the real drain barrier at all.** First reproduction attempt (looping iterations inside one persistent process) showed 0/16 failures, not the documented ~3/8 -- a methodology gap, corrected by spawning 8 separate fresh Python processes per the original characterization, which reproduced it on the first attempt (1 failure at iteration 2 of run 1). Instrumented timing confirmed the item genuinely reaches `transcript_queue` well before the drain-barrier check runs (the 60ms inactivity timer fires at t≈71-77ms, the drain check at t≈95-99ms), which ruled out a real race in the commit path itself and pointed at the drain step's own mechanics instead. |
| 2026-08-19 | Sonnet 5 | **The actual defect: `tests/test_task9_report.py`'s `RealIntegrationHost` -- documented in its own docstring as "the smallest real subset wiring main_window.py, stop_finalize_worker.py, and translation_worker.py together as in production" -- never borrows `_flush_pending_transcript_queue` or `drain_transcript_queue_for_stop` from `AlphaApp`.** Confirmed directly (`hasattr(host, ...)` both `False`). `alpha/utils/ui_stop_drain_barrier.py`'s `drain_stop_queues_on_main_thread` gates BOTH its real drain attempts behind exactly those two `hasattr` checks, so for this host the "drain" step never flushes anything -- it only reads `transcript_queue.qsize()`. The only thing that ever empties the queue is `process_ui_queue()`'s own independent `UI_UPDATE_INTERVAL_MS=100`ms poll, which the test never coordinates with the drain-barrier check that runs at ~95-99ms -- close enough to the 100ms boundary that whichever side wins is essentially a coin flip, matching the measured ~3-of-8 rate exactly. The test was not catching a production race; it was never wiring up the code path it claims to cover. |
| 2026-08-19 | Sonnet 5 | **Fixed by completing the wiring, the same pattern already used for every other borrowed method on this host** — `_flush_pending_transcript_queue`, `drain_transcript_queue_for_stop`, `_enqueue_transcript_ui_batch`, `_flush_transcript_ui_batch`, `_schedule_transcript_ui_batch_flush` borrowed from `AlphaApp`, plus the batch-buffer state they depend on (`_transcript_ui_batch_buffer` and friends) initialized per-session, with the same cancel-pending-job handling the file already uses for `_process_ui_queue_after_id` since this host's Tk root persists across iterations. Verified three ways: the real `pytest` test across 10 separate fresh-process invocations (50 subtest-iterations), 0 failures, against a documented ~3-of-8 pre-fix rate; a probe confirming the drain now actually empties the queue (put 1 item, flush, qsize 0); and the full suite run on both the stashed pre-fix tree and the fixed tree -- identical 777 tests, 6F+2E+2S, same names both times, proving no regression and that the extra (6th, beyond the previously-documented 5F+2E) failure is pre-existing suite flakiness unconnected to this change (today's varying name was `test_stop_finalize_v3_2_3...test_phase_constants_match_spec`, not the previously-recorded `test_item48_audio_manifest_bounded...` -- same known flakiness class item 78 itself predicted, a different name this time, not a new one). |
| 2026-08-16 | Opus 5 | **Items 46 and 47 are REOPENED: both shipped as library code with no caller.** Found while scoping item 73, which was supposed to surface a device change "the way item 47's status indicator surfaces connection state" — there is no indicator. `alpha/utils/service_status.py` is imported by nothing in the app; all six exported names return **0 non-defining references** across `alpha/` and `main.py`. No credential preflight runs at Start, no status widget exists. The functions are correct and tested; only the wiring is missing. **Third instance of this exact shape** — item 44's `commit_in_flight` was never implemented, item 65's log sat behind a gate that returned first, and now these two. **Standing rule this earns: before closing an item, grep for a production CALLER, not just for the code.** A passing test proves the function works, never that anything invokes it. |
| 2026-08-16 | Opus 5 | **Item 70's widening was implemented and then WITHDRAWN before shipping. Three findings, all reproduced against the real lifecycle, all confirmed a second time by me rather than taken on report.** **(1) Duplication:** after a flush the next cumulative window carries the committed sentence back in; item 66's `_strip_committed_tail_prefix(min_run=3)` fails closed below 3 tokens, so `"Hello there."` committed, then committed again inside `"Hello there. How are you doing?"`. Threshold is exact — 1-2 token sentences duplicate, 3+ do not — which makes short acknowledgements the worst case, not a corner case. **(2) Content loss:** the flush tail reaches `_trim_resent_tail_locked` via `force_new=True`, a trim written for a window the provider RE-SENT, which the tail provably is not; ordinary anadiplosis (`"...the bottom line. The bottom line is simple."`) lost `"The bottom line"` entirely. **(3) It did not work:** longest record unchanged at 2084 chars, records >400 went 59→60. **Root obstacle: 68% of finals do not end on a terminator**, so the boundary is interior while the gate tests `prev[-1]`. Shipped the detection half alone (`22aafad`) and pinned both traps as guards, verified non-vacuous by monkeypatching the withdrawn predicate back in and confirming all three fail. |
| 2026-08-16 | Opus 5 | **The detection half was worth shipping on its own, and the reason generalises.** `AlphaApp._display_transcript_item`'s `is_final is False` early return was completely silent, and `duplicate_protection`'s logged copy of the same gate is unreachable for it — the app method returns first. `tests/test_committed_segment_is_final.py` could not see that because it binds the mixin onto a bare host and never executes the app method: **a green test over a dead production log.** Worth remembering as a shape: a test that binds a mixin method directly cannot cover a gate the real class puts in front of it. |
| 2026-08-16 | Opus 5 | **A pre-existing `_merge_lexical` defect found while widening the flush, NOT fixed (no drive-by, §0 rule 3).** For `prev="I am very happy."` and a revision `curr="I am very happy today."`, `_merge_lexical` returns **`"I am very happy. today."`** — it keeps the withdrawn period and mangles the sentence. This is independent of item 70: before the widening the mangled text simply sat in the active utterance and was committed as one record. It surfaced because a startswith-only flush test split at that stale period, breaking `test_a_revision_is_never_split`. Worth its own item; the flush now refuses the shape via `prev in curr` rather than depending on the merge being correct. |
| 2026-08-15 | Sonnet 5 | **NOT FIXED — a hole in item 67, found by review, not by a run.** `serialize_export_payload`'s trailing-gap branch is `if pending_gaps and lines:`. When a session has connection gaps but **no exportable record**, `lines` is empty, the branch is skipped, and every gap is dropped with no trace. Driven through the real function: with one record, body = `[connection lost - approximately 45s of audio not captured]\nSpeaker: hello there`; with **zero** records and the same gap, body = `''`. Reachable exactly when it matters most — the network dies before anything commits, the user presses Stop, and the client is handed a blank file with no indication the connection ever failed, which is the scenario items 44/67 exist for. Left unfixed per §0 rule 3 (no drive-by); needs its own item. |
| 2026-08-14 | Opus 5 | **Both fixed by merging gaps that land on the same record into one marker carrying the total** — which is also the more truthful rendering: with no committed speech between them, consecutive drops are ONE hole in the transcript and its size is the sum. 10 s + 40 s now reads 50 s; two 31 s drops read 62 s; gaps separated by real speech stay separate. Also verified against the export pipeline rather than assumed: `cleanup_punctuation_artifacts` leaves the marker byte-identical, `build_final_export_record_rows` reads `snap["records"]` so the marker never enters a canonical record or its `content_sha256`, and `lines` stays 1:1 with `record_ids`. Suite 670 → 674. |

---

## 10. Model selection reference

| Work | Model | How |
|---|---|---|
| Ambiguous root-cause investigation, long multi-file work (41, 42) | Claude Fable 5 | `/model fable` then `/effort xhigh`. Describe the outcome, not the steps — it investigates before acting and verifies itself, so verification reminders are wasted tokens |
| Architecture, concurrency, threshold judgment | Opus 5 | `claude --model opus`; add `ultrathink` in the prompt for one-off deeper reasoning |
| Template fixes, tooling, tests, packaging | Sonnet 5 | `claude --model sonnet` |
| Mixed planning + execution | `opusplan` | Opus while planning, Sonnet for execution |

`/model` opens the picker, `/status` shows what is active, and `--model`
applies per session — so a Fable session and a Sonnet session can run in
two terminals at the same time.
Reference: https://code.claude.com/docs/en/model-config

---

## 11. Item 49 — delivery mechanism: what was measured, and what decides it

Written 2026-08-16 so the decision can be made from facts rather than
re-derived under time pressure. **Nothing here is built yet.** The target
is a hand-over onto a non-developer's (the boss's) Windows laptop.

### 11.1 The blocker, stated plainly

**No packaged build exists.** No `.spec`, no installer script, nothing in
`tools/` that produces an artifact. §5's "three full sessions **from the
installed build**" has nothing to run from. Item 49 is therefore not
"verify the install" — it is "decide the delivery mechanism, build it,
then verify it".

### 11.2 What the target machine actually needs

Measured from `requirements.txt`, `requirements-lock.txt`, `.venv/pyvenv.cfg`
and `alpha/config.py`:

| | Detail |
|---|---|
| Python | Lock header says **3.14.6**; shipped code uses `str \| None`, so the *language* floor is 3.10, but every locked wheel is built for 3.14 |
| tkinter | Bundled with the python.org Windows installer — verified present, no separate install |
| pip packages | customtkinter 5.2.2, Pillow 12.2.0, sounddevice 0.5.5, websocket-client 1.6.0, pyaudiowpatch 0.2.12.8, numpy 2.5.0, python-dotenv 1.0.0, requests 2.34.2, psutil 7.2.2 (+ `deepl`, see the lock gap below) |
| Not software, still required | `.env` at the repo root with `DEEPGRAM_API_KEY` and `DEEPL_API_KEY` (`config.py:14` loads it); internet for Deepgram's websocket and DeepL |
| Not required | Git — a ZIP is enough |

**Lock file gap, found 2026-08-16:** `requirements-lock.txt` pins **9**
packages and **omits `deepl`**, which `requirements.txt` requires. It also
pins no transitive dependencies (urllib3, charset-normalizer, …), so the
name "lock" overstates it. Fix this before any packaging work — every
option below installs from it.

**`.venv` is not portable.** `pyvenv.cfg` hardcodes
`home = C:\Users\islamm\AppData\Local\Python\pythoncore-3.14-64` and the
full venv path. It has already broken once on a user-account move (§3).
Copying `.venv` to another machine is not a delivery option.

### 11.3 The four options

| | Boss's effort | Admin needed | Antivirus risk | Build effort | Size |
|---|---|---|---|---|---|
| **A. Fresh clone + venv, manual** | installs Python, runs commands | maybe | low | none | small |
| **B. `.bat` + system Python** | one installer prompt | maybe | low (installer) | ~1–2 h | small |
| **C. `.bat` + embeddable Python bundle** | **none — double-click** | **no** | low | medium | ~60–80 MB |
| **D. PyInstaller `.exe`** | **none — double-click** | no | **highest** (unsigned self-extractor) | high | 150+ MB |

### 11.4 The two facts that decide it — both currently UNKNOWN

Neither has been measured. Measure both before choosing; they are cheap
and they eliminate whole options.

1. **Can software be installed on the boss's laptop at all?** (admin
   rights, IT policy, whether `pip` can reach the network through a
   proxy.) If **no**, A and B are dead and it must be C or D.
2. **Does PyInstaller support Python 3.14?** PyInstaller is **not
   installed here**, and 3.14 is new enough that support may lag. If it
   does not, D requires rebuilding the whole dependency set on 3.12/3.13
   — including a `pyaudiowpatch` wheel for that version, itself unknown.

### 11.5 Why D (single `.exe`) is not simply the best answer

It is the nicest experience and the riskiest build, and the risk is
concrete rather than general:

**Every path anchor in the app is `Path(__file__)`.** Under a onefile
build the code runs from a temp extraction (`sys._MEIPASS`), so these all
point somewhere that is deleted on exit:

```
alpha/config.py:10              PROJECT_ROOT   = Path(__file__).resolve().parent.parent
alpha/transcription/deepgram_client.py:133  _PROJECT_ROOT = Path(__file__).resolve().parents[2]
alpha/utils/async_debug_log.py:17           (same shape)
alpha/utils/crash_guard_log.py:21           (same shape)
alpha/utils/diagnostic_test_log.py:47       (same shape)
alpha/utils/freeze_guard_log.py:20          (same shape)
```

Consequences, not hypotheticals: `.env` is looked for inside the temp
extract, so **the boss's API keys are never found**; and
`troubleshooting/runs/` evidence is written into that temp directory and
**deleted when the app exits**, which removes the entire evidence trail
this project's debugging depends on.

Making these frozen-aware (`sys.frozen` → beside `sys.executable`) is a
small change, but it touches six shipped modules days before freeze.

**`--onedir` (a folder with the `.exe` inside) avoids most of this** —
`sys._MEIPASS` still applies but the folder is stable, `.env` can sit
beside the executable, and evidence has somewhere durable to go. If the
answer to "can files live beside the exe?" is yes, prefer onedir over
onefile.

### 11.6 Recommendation, and what to do first

**Option C** unless fact (1) rules out an install entirely, in which case
**D as `--onedir`, not `--onefile`.** C gives the boss the same
double-click experience as D, needs no admin, avoids the unsigned-binary
antivirus problem, and — decisively — **requires no change to shipped
code**, so it cannot introduce a path bug days before freeze.

C's one unknown: the embeddable Python zip ships **without tkinter**, so
`_tkinter.pyd`, `tk86t.dll`, `tcl86t.dll` and the `tcl/` tree must be
copied in from a full install. That works, but must be verified rather
than assumed.

Order of work, cheapest-first, all of it doable without the target
machine:

1. Fix the `requirements-lock.txt` gap (add `deepl`, pin transitives).
2. Build a throwaway venv from the lock on a different path and confirm
   every wheel installs on 3.14 — **`pyaudiowpatch` is the one most
   likely to fail**, and it is the hardest to work around because it is
   the WASAPI loopback capture layer.
3. Check whether PyInstaller supports 3.14, in a *separate* venv so the
   project's baseline venv stays untouched.
4. Only then choose, and build.

### 11.7 A separate delivery risk, bigger than packaging

`alpha/audio/wasapi.py:33` calls `get_default_wasapi_loopback()` — there
is **no device picker**. The app captures whatever Windows currently has
as its default output device. On a laptop with a headset, a dock or
Bluetooth, that is whatever was last selected, and **changing audio
device mid-meeting silently moves the capture source**.

§1's problem **D** listed "device change" alongside network drop, quota
and credentials; items 44/45/46 covered the other three, and this one was
never picked up. It will bite on any machine other than the development
one, whichever delivery option is chosen. Worth filing as its own item.

| 2026-08-19 | Opus 5 | **Item 70 closed by splitting at an interior terminator; the symptom was reproduced against the real lifecycle before a line was written.** `raw_deepgram_finals.jsonl` was never retained and `provider_events.jsonl` is post-merge on the English path, so neither replays as provider input — but a committed record's text IS the accumulated text, so re-growing the 25 longest real English records word by word reproduces the cumulative shape with real terminator positions. Pre-fix: **25 in, 25 out, longest 1982, all 25 over 400, zero splits.** Post-fix: **443 out, longest 476, 2 over 400, 0 words lost, 0 gained.** |
| 2026-08-19 | Opus 5 | **The condition the withdrawn attempt was missing is `curr.startswith(head)` — the provider's OWN latest text must still carry the completed sentence.** The other three conditions (terminator with text on both sides, byte-exact reconstruction, survival across a merge) all pass on `"I am very happy. today."`, the shape `_merge_lexical` produces when it keeps a period the provider withdrew. Only checking the incoming text refuses it. Recorded because the earlier attempt widened the test to `prev in curr` and split at the seam anyway, which is a different question from *where* the settled boundary is. |
| 2026-08-19 | Opus 5 | **A silent identity rejection ate every interior split, and only tracing the real commit found it.** Shortening `active.text` to the committed head without bumping `active.version` presents different text at a version `_observe_identity` has already recorded, which it refuses as `conflicting_same_version_text`. The split predicate fired on every chunk and nothing was ever published — no exception, no log, just no output. Measured: without the version bump the replay produces 34 records with the longest still 1909 chars, i.e. it looks like a weak fix rather than a broken one. **Shape worth remembering: a fix that "half works" in this module is more likely to be fully rejected downstream than partially correct.** |
| 2026-08-19 | Opus 5 | **Item 66's re-send trim began deleting genuine speech once splits started, and it is a fuzzy comparison in a place that now needs an exact one.** `_strip_committed_tail_prefix` matches token runs, so it cannot distinguish a provider re-send from a speaker repeating themselves — real record `"...Oh, yeah. So everybody agrees. So everybody agrees. Yeah..."` lost one whole phrase. A split commit ends on a terminator by construction, so there is no partial span to re-send; the cumulative re-send it does produce is removed exactly by the new committed-prefix marker. `sentence_boundary_flush` therefore joins `provider_disconnected` in `_HARD_BOUNDARY_COMMIT_REASONS`. Measured: with the trim re-armed, 9 words are lost across the same 25 records. |
| 2026-08-19 | Opus 5 | **Every part of the fix was verified by neutralising it alone, not by asserting the whole worked.** Interior split off → 25 records / 1982 max (the original symptom exactly). Version bump off → 34 / 1909. Fuzzy trim re-armed → 9 words lost. Exact prefix-strip off → `"Hello there."` committed three times (the withdrawn attempt's duplication trap, verbatim). Also measured and rejected: taking the LAST qualifying terminator instead of the first sends four settled sentences out as one record — 14 committed records still over 400, longest 879. |
| 2026-08-19 | Opus 5 | **CRLF hazard hit again, this time from a verification script rather than an edit.** Rewriting the source with Python text mode (`encoding="utf-8"`, even with `newline=""`) converted the whole file CRLF → LF and produced a 3193/3016 whole-file diff; a `subprocess.run` with a relative venv path then failed and left the file patched mid-experiment. Restored in BINARY mode and verified 3193 CRLF / 0 bare LF. **Check `git diff --stat` after any scripted edit to a `.py` file in this repo — a line-ending flip is invisible in the editor and unreviewable in the diff.** |

| 2026-08-19 | Opus 5 | **Item 74 was verified rather than assumed, and its severity was wrong in both directions.** The user believed it was already complete; the ledger called its three parts "existing defects". Measured against the real code, all three are **LATENT**: the render cap holds exactly (620 segments against a cap of 500 left 500 lines, zero drift), a completed translation is one logical line, and a real segment insert costs **0.009 ms** so the missing flush budget has ~2700x of headroom. Nothing in item 74 was broken today. Only (c) was fixed, because a time check in a loop is cheap and removes the failure mode item 75 walks into; (a) and (b) were left alone and their one-logical-line invariant pinned by tests instead, since re-deriving arithmetic for a shape that does not exist is the drive-by §0 rule 3 forbids. |
| 2026-08-19 | Opus 5 | **Verifying item 74 surfaced THREE live bugs it never named, all one defect: a Tk mark set at `"end"` does not bound the text written after it.** `"end"` is one character past where `insert("end", ...)` writes, because Tk keeps a trailing newline of its own, and the default RIGHT gravity carries the mark along with text inserted at its position — so the mark lands AFTER the new text and `delete(mark, "end")` spans an empty range. **This is the opposite of what `UI_CHANGE_BASELINE_AUDIT.md` trap 3 and item 75's filing both claimed** (they said appended text lands INSIDE the range); both documents were corrected. Fixed in `f59b99c` and `fbc555d`: **`interim_anchor`** — the preview was never removed, four ticks left four stacked ⏳ rows in the live pane; **`segment_anchor`** — a corrected segment was appended to its predecessor on one line with no separator; **the completed-translation `tr_done_*` mark** — a revision left the superseded translation on screen, and the same index mis-sized the `speaker_label` tag range. |
| 2026-08-19 | Opus 5 | **The working recipe, since three sites got it wrong independently:** re-establish an empty last line so the position is a line start, mark `"end-1c"` rather than `"end"`, and set **LEFT** gravity. All three parts are required — measured over three ticks, `"end"` alone, `"end-1c"` alone, `"end"` + left, and `"end-1c"` + right each still stacked three previews; only the full combination produced one. The newline guard is the non-obvious part: the delete consumes Tk's own trailing newline, after which `"end-1c"` resolves into the MIDDLE of the last committed line rather than the start of an empty one — traced as index 2.0 on the first tick and 1.19 on the second. A fourth instance survives at the pending-placeholder mark and is left alone only because `TRANSLATION_PENDING_PLACEHOLDER_VISIBLE` is `False`; fix it with this recipe before that flag is ever re-enabled. |
| 2026-08-19 | Opus 5 | **All three bugs were invisible to the existing tests by construction, which is the reusable lesson.** Every one of them only appears when the REAL app method writes into a REAL `tk.Text`. The existing coverage binds a mixin or substitutes a fake widget, and the audit records that both Tk-touching test files are skipped under `SKIP_TK_INTEGRATION_TESTS=1` — so the suite was green while the pane a user watches was stacking previews. The three new test files build their own headless Tk root and deliberately do NOT honour that flag; a flag-gated version would not have caught the bugs it exists for. Against the pre-fix tree they fail 8, 3 and 4 of their tests respectively. |

| 2026-08-19 | Opus 5 | **Item 70 as first shipped was a regression, caught by the user's live run, and the correction is that a record boundary must equal a READABLE LINE.** Splitting at the first settled sentence made every record one sentence: median line length fell from **24 words to 6** and lines of 5 words or fewer went from **6% to 45%** ("I don't know.", "That's all I got." each became a line). The rule is now the one already encoded in `english_line_grouping.py` and asked for by the user — 2-3 sentences, ~20 words — reusing `ENGLISH_LINE_MIN_SENTENCES` / `ENGLISH_LINE_TARGET_WORDS` / `ENGLISH_LINE_MAX_SENTENCES` rather than inventing thresholds, because if the record boundary and the rendered line disagree the text reflows the moment it commits, which is item 69's bug. Replaying the live run's own speech: median back to **26 words**, short lines **45% → 7%**, longest record 700 chars against 1556 pre-item-70. **Both extremes had to be measured to find this**: last-qualifying-terminator left 14 records over 400 chars, first-qualifying gave 6-word lines. |
| 2026-08-19 | Opus 5 | **Measurement caveat on that replay, stated because it overstates the gain.** It re-grows the run's committed text as ONE continuous cumulative stream, so it never reproduces the `utterance_end` / inactivity commits that fire on a speech pause. Those produce the mid-phrase records filed as item 79, and the real improvement in a live session will be smaller than 45% → 7%. The next live run is the only honest measurement. |
| 2026-08-19 | Opus 5 | **The missing scrollbar was Tk pack order, not styling or the item 71 width pins.** `_create_styled_text` packs the text widget first with `expand=True`, so it claims the entire cavity; `check_scrollbar_visibility` packs the scrollbar afterwards and gets what is left, which is nothing. Measured on a REALISED window (an unmapped root reports width 1 for everything and makes this test vacuous): packed after → width **1**, `winfo_ismapped()` **False**; packed before → width **16**, mapped, text widget giving up exactly those pixels. The summary panel packs its scrollbar FIRST and always had one, which is what made this look like it could not be pack order. Fixed with `before=text_widget` on the SHOW path — it cannot live at creation only, because the auto-hide calls `pack_forget()` and re-packing appends to the end of the order again. The function wraps everything in `except Exception: pass`, so nothing would have been logged either way. |
| 2026-08-19 | Opus 5 | **Correcting my own item 74(c) claim from earlier today: the flush budget is NOT purely latent.** That entry said a real insert costs 0.009 ms and the branch was unreachable. 0.009 ms is the bare `_insert_speaker_segment_line`; the full `_display_transcript_item` — store write, translation submission, canonical commit — is far heavier. The live run logged `TRANSCRIPT_UI_FLUSH_TIME_BUDGET_EXCEEDED` **5 times in 25 minutes**, every one of them `rendered: 1, deferred: 1` at **12.9-18.0 ms** for a SINGLE item. So the guard is doing real work in production rather than waiting for item 75, and the deferral is behaving exactly as designed: one item rendered, one carried to the next tick, nothing dropped. `UI_EVENT_DRAIN_TIME_BUDGET_EXCEEDED` fired 30 times, all `processed: 1, remaining: 0` — the tick ran long on one item with an empty queue behind it, which is benign. |

| 2026-08-20 | Opus 5 | **The word loss was never in the transcription pipeline, and measuring the stages separately is what showed it.** Reference 2186 words, Deepgram returned 1106 (43.1%), export kept 1067 of those 1106 (96.5%). Blaming the lifecycle or the merge would have been the obvious reading and would have been wrong. The decisive artifact was the retained WAV set: the `mixed` stream is written at `main_window.py:7638` and its own comment calls it the "exact mixed Deepgram-delivery PCM", so comparing it against the pre-mix `system` stream located the loss in one step — system steady at 2-15% silence, delivered 52-82%. |
| 2026-08-20 | Opus 5 | **A replay that exonerates a component can be wrong in a way that looks like evidence.** The first replay of `TeamsSourceGate` over the real audio showed only 0-9.5% gated silence and appeared to clear it. It passed the real `time.monotonic()` as `now`, so thousands of frames elapsed inside one 500 ms `SOURCE_HOLD_MS` window and `_apply_hold` never let the source change. Re-run with a clock advancing 20 ms per frame, the same gate reproduced the live failure within a few percent. **Any replay of a time-gated component must simulate the clock at the real cadence** — otherwise every hold, debounce and TTL in the path silently behaves differently than in production. |
| 2026-08-20 | Opus 5 | **Translation is NOT slow and must not be "optimised" — measured, so the request could be answered with numbers rather than an opinion.** From `translation/translation_events.jsonl`, 132 jobs, 0 failures: DeepL provider median **384 ms** / p95 654 ms, queue wait **0 ms**, ordering wait 12 ms, end-to-end median **374 ms**, UI update 67 ms. Everything outside DeepL's own round trip is about 80 ms. The perceived slowness was the transcript arriving late and in fragments because of item 80's audio loss. The only real lever would be batching, which costs the DeepL context item 50 added, so it was rejected rather than attempted. |
| 2026-08-20 | Opus 5 | **Readability on that run was 122 records, median 3 words, 63% at <=5 words, and only 17 of 122 ending on a terminator.** Item 70's readable-line rule cannot help when 86% of records are mid-phrase — that is item 79, and item 79 is itself downstream of item 80: audio arriving as 52-82% silence produces fragmentary finals. Re-measure readability only after a live run on the fixed audio path, since fixing item 80 may resolve most of item 79 without touching the commit logic. |
